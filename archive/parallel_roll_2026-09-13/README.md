# 归档：并行 2 日滚动实现（2026-09-13）

本目录保存已退役的**并行实现**（原 `program/src/solve/q2_roll.py`、`q3_roll.py`），
它们在 2026-09-13 的"双轨合流"后不再参与官方结果生成，故归档删除。

## 退役原因

- 官方 Q2/Q3 的 2 日滚动已由主线模块实现：
  - Q2：`solve.q2_tune.simulate_two_day` + `write_result2_tuned`（`uv run python -m solve.q2_tune --result2`）
  - Q3：`solve.q3_proto.plan_two_day` + `solve.q3`（`uv run python -m solve.q3`）
- 两套实现的规模、参数与初始条件不同（本机版 κ=1.02/m=50、仿真自 1-16 起；主线为
  时间留出选参、报送期直接起算），并在相同参数下给出**一致**的年末费用
  （Q2：1405.7 万 vs 1405.7 万），交叉验证完成后按"一套正式实现"原则归档。

## 归档前保留的对照结论（论文可引用的结构证据）

- Q2 储能结构对比（`--probe`）：日循环 1406.6 万 / 2 日滚动·末端锚定 1405.7 万 /
  2 日滚动·末端自由 1405.7 万；末端条件只影响规划窗口最后约 6 个槽的充放，
  执行日（首日）结果完全相同。
- Q3 结构/执行窗口对比（`--probe`）：6 h 执行窗 1326.4 万 < 12 h 执行窗 1333.7 万（+0.55%）；
  末端锚定与自由同值。注：该组数字为并行实现（实际负荷口径、10 情景）下的结果，
  与后续主线（因果负荷预测、40 情景）口径不同，仅作结构对照。

## 复现方式

```powershell
# 恢复脚本（如需重跑对照）：
copy archive\parallel_roll_2026-09-13\q2_roll.py program\src\solve\
copy archive\parallel_roll_2026-09-13\q3_roll.py program\src\solve\
# 注意：恢复后需先确认其导入的接口（q2.plan_horizon 等）与当前主线签名一致。
```

归档日期：2026-09-13；归档时工作区已统一为"2 日规划窗口 + 1 日/6 h 执行、储电量
跨日连续、规划窗口末端完全自由（不锚定终值）"的正式口径。
