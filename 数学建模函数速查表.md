# 数学建模 Python 函数速查表（SciPy / SymPy / PyTorch）

> 基于当前主流版本编写并实测：**scipy 1.18 / sympy 1.14 / torch 2.14**（大多数签名在 1.8 / 1.12 / 2.0+ 通用）。
> 每个核心函数给出：**函数签名 → 各参数作用 → 返回值用法 → 可运行示例**；使用频率较低的函数以速查表形式列出。

```python
# 推荐导入约定（全文通用）
import numpy as np
import scipy.optimize as opt
from scipy import integrate, interpolate, stats, linalg, signal, sparse, spatial
import scipy.cluster.hierarchy as sch          # 层次聚类
from scipy.spatial.distance import cdist, pdist, squareform
import sympy as sp
import torch
import torch.nn as nn
import torch.nn.functional as F
from torch.utils.data import DataLoader, TensorDataset
```

---

## 目录

1. [SciPy](#一scipy)
   - [1.1 optimize 优化与方程求解](#11-scipyoptimize-优化与方程求解)
   - [1.2 integrate 积分与微分方程](#12-scipyintegrate-积分与微分方程)
   - [1.3 interpolate 插值](#13-scipyinterpolate-插值)
   - [1.4 stats 统计检验与概率分布](#14-scipystats-统计检验与概率分布)
   - [1.5 linalg 线性代数](#15-scipylinalg-线性代数)
   - [1.6 sparse 稀疏矩阵](#16-scipysparse-稀疏矩阵)
   - [1.7 cluster 聚类](#17-scipycluster-聚类)
   - [1.8 spatial 空间计算](#18-scipyspatial-空间计算)
   - [1.9 signal 数据平滑与信号](#19-scipysignal-数据平滑与信号)
2. [SymPy](#二sympy-符号计算)
3. [PyTorch](#三pytorch)
4. [附录：建模场景 → 函数映射](#四附录)

---

# 一、SciPy

## 1.1 scipy.optimize 优化与方程求解

### `linprog` — 线性规划（默认求最小值）

```python
scipy.optimize.linprog(c, A_ub=None, b_ub=None, A_eq=None, b_eq=None,
                       bounds=(0, None), method='highs', callback=None,
                       options=None, x0=None, integrality=None)
```

| 参数 | 作用 |
| --- | --- |
| `c` | 一维价格向量，目标 `min cᵀx`。**求最大值时传 `-c`**，结果取 `-res.fun` |
| `A_ub`, `b_ub` | 不等式约束 `A_ub @ x <= b_ub` |
| `A_eq`, `b_eq` | 等式约束 `A_eq @ x == b_eq` |
| `bounds` | 每个变量的 `(下界, 上界)` 序列，**默认全部 `x≥0`**；无界用 `None`，如 `[(0,10),(None,0),(None,None)]` |
| `method` | `'highs'`（默认，推荐）、`'highs-ds'`、`'highs-ipm'`、`'revised simplex'` |
| `integrality` | 与 `c` 等长数组：`0` 连续、`1` 整数（配合 HiGHS 可直接解整数线性规划，新版本支持） |
| `options` | 字典，如 `{'disp': True, 'time_limit': 60}` |

**返回值** `OptimizeResult`：
- `res.x`：最优解向量；`res.fun`：最优目标值；`res.success`：是否求解成功（bool）
- `res.status` / `res.message`：状态码与说明（0=成功）
- `res.ineqlin.marginals` / `res.eqlin.marginals`：约束对偶价格（灵敏度分析用）

```python
res = linprog([-3, -5],                          # max 3x+5y → min -3x-5y
              A_ub=[[1,0],[0,2],[3,2]], b_ub=[4,12,18],
              bounds=[(0,10),(0,10)])
x, z = res.x, res.fun                            # x=[2.,6.], z=-36.0 → 最大值 36
```

### `milp` — 混合整数线性规划（MILP）

```python
scipy.optimize.milp(c, *, integrality=None, bounds=None,
                    constraints=None, options=None)
```

| 参数 | 作用 |
| --- | --- |
| `c` | 目标系数（最小化；最大化取负） |
| `integrality` | 数组：`0` 连续、`1` 整数、`2` 半连续、`3` 半整数 |
| `bounds` | `Bounds(lb, ub)`，各变量上下界（可含 `np.inf`） |
| `constraints` | `LinearConstraint(A, lb, ub)` 的列表，表达 `lb <= A@x <= ub` |
| `options` | `{'time_limit': 秒, 'mip_rel_gap': 相对间隙, 'disp': True}` |

**返回值**：`res.x`、`res.fun`、`res.success`、`res.mip_gap`、`res.mip_node_count`

```python
from scipy.optimize import milp, LinearConstraint, Bounds
res = milp(c=[-3,-5],
           constraints=[LinearConstraint([[1,0],[0,2],[3,2]], -np.inf, [4,12,18])],
           bounds=Bounds([0,0],[10,10]), integrality=[1,1])
print(res.x, res.fun)          # [2. 6.] -36.0
```

### `minimize` — 通用非线性优化（核心函数）

```python
scipy.optimize.minimize(fun, x0, args=(), method=None, jac=None, hess=None,
                        hessp=None, bounds=None, constraints=(), tol=None,
                        callback=None, options=None)
```

| 参数 | 作用 |
| --- | --- |
| `fun` | 目标函数 `f(x, *args)`，`x` 为 ndarray，返回标量 |
| `x0` | 初值（决定收敛性的关键，多峰问题建议多初值重试） |
| `args` | 额外传给 `fun` 的参数元组 |
| `method` | 见下表 |
| `jac` | 梯度函数，或 `'2-point'`（数值梯度）；多数方法给了会快很多 |
| `bounds` | 变量上下界序列 `[(lo,hi),...]`（`L-BFGS-B`/`TNC`/`SLSQP`/`trust-constr` 支持） |
| `constraints` | `SLSQP`/`COBYLA`：字典列表 `{'type':'eq'/'ineq', 'fun': g, 'jac': ...}`，`'eq'` 表示 `g(x)=0`，`'ineq'` 表示 `g(x)>=0`；`trust-constr`：`LinearConstraint`/`NonlinearConstraint` 对象 |
| `options` | `{'maxiter':, 'disp':, 'gtol':, 'ftol':}` 等，随 method 不同 |
| `callback` | 每步回调 `cb(xk)`，可记录迭代轨迹 |

| method | 特点 | 支持约束 |
| --- | --- | --- |
| `'Nelder-Mead'` | 单纯形法，不需梯度，抗噪声 | 无 |
| `'BFGS'` | 拟牛顿，光滑问题首选 | 无 |
| `'L-BFGS-B'` | 内存友好、带边界，最常用默认之一 | 边界 |
| `'SLSQP'` | **约束优化最常用**，边界+等式/不等式 | 全 |
| `'trust-constr'` | 大规模约束问题，精度高 | 全（对象式） |
| `'Powell'` / `'CG'` / `'TNC'` / `'COBYLA'` / `'Newton-CG'` | 备选 | 部分 |

**返回值** `OptimizeResult`：
- `res.x` 最优解；`res.fun` 最优值；`res.success`；`res.nit` 迭代次数
- `res.jac` 最优点梯度（≈0 说明收敛）；`res.hess_inv` Hessian 近似

```python
# min (x-1)^2 + (y-2)^2  s.t. x+y=3
res = opt.minimize(lambda p: (p[0]-1)**2 + (p[1]-2)**2, x0=[0,0],
                   constraints=[{'type':'eq', 'fun': lambda p: p[0]+p[1]-3}])
print(res.x, res.fun)
```

### `minimize_scalar` — 一维优化

```python
scipy.optimize.minimize_scalar(fun, bracket=None, bounds=None, args=(),
                               method=None, tol=None, options=None)
```

- `bracket`：三点初始括号（`method='brent'` 时加速收敛）
- `bounds`：`(a, b)`，`method='bounded'` 时**必给**，在界内搜索
- `method`：`'brent'`（默认，无界）、`'bounded'`、`'golden'`

**返回值**：`res.x`（最优自变量）、`res.fun`（最优值）、`res.success`

```python
res = opt.minimize_scalar(lambda x: (x-3)**2, bounds=(0, 10), method='bounded')
res.x          # ≈ 3.0
```

### `differential_evolution` — 全局优化（差分进化）

```python
scipy.optimize.differential_evolution(func, bounds, args=(), strategy='best1bin',
    maxiter=1000, popsize=15, tol=0.01, mutation=(0.5, 1), recombination=0.7,
    rng=None, callback=None, disp=False, polish=True, init='latinhypercube',
    atol=0, updating='immediate', workers=1, constraints=(), x0=None,
    *, integrality=None, vectorized=False, seed=None)
```

| 参数 | 作用 |
| --- | --- |
| `bounds` | `[(lo,hi), ...]` 每维搜索范围（必给） |
| `maxiter` / `popsize` | 最大代数 / 种群倍数（种群大小 = popsize × 维数） |
| `strategy` | 变异策略，如 `'best1bin'`、`'rand2exp'` |
| `polish` | 结束时用 L-BFGS-B 局部精修（默认开） |
| `workers` | `-1` 表示用全部 CPU 并行 |
| `integrality` | 某些维取整数 |
| `seed` | 随机种子（保证结果可复现，论文必设） |

**返回值**：`res.x`、`res.fun`、`res.success`、`res.nit`、`res.message`

```python
res = opt.differential_evolution(lambda p: (p[0]-2)**4 + (p[1]+1)**2,
                                 bounds=[(-10,10),(-10,10)], seed=42)
```

> 同类全局优化：`shgo`（凸+全局，可返回所有极小）、`dual_annealing`（模拟退火）。

### `curve_fit` — 非线性最小二乘拟合（核心函数）

```python
scipy.optimize.curve_fit(f, xdata, ydata, p0=None, sigma=None,
    absolute_sigma=False, check_finite=None, bounds=(-np.inf, np.inf),
    method=None, jac=None, *, full_output=False, nan_policy=None, **kwargs)
```

| 参数 | 作用 |
| --- | --- |
| `f` | 模型函数 `f(x, a, b, ...)`：第一个参数是自变量，其余为**待拟合参数** |
| `p0` | 参数初值列表。**强烈建议显式给出**，否则用 1.0，容易不收敛 |
| `sigma` | y 的标准差（加权拟合）；`absolute_sigma=True` 时 `pcov` 直接按 σ 解释 |
| `bounds` | `(下界数组, 上界数组)`；一旦设置，`method` 自动变为 `'trf'` |
| `method` | `'lm'`（无界时默认）/`'trf'`/`'dogbox'` |
| `full_output=True` | 额外返回 `(infodict, mesg, ier)` 拟合诊断信息 |

**返回值** 二元组 `(popt, pcov)`：
- `popt`：拟合出的参数数组
- `pcov`：参数协方差矩阵，**参数标准误 = `np.sqrt(np.diag(pcov))`**（写论文报误差用）
- 拟合优度 R²：用 `popt` 算 `y_pred` 后 `1 - SS_res/SS_tot`（见下例）

```python
f = lambda t, a, b, c: a*np.exp(-b*t) + c
popt, pcov = curve_fit(f, t, ydata, p0=[1, 1, 0])          # → [2.526 1.309 0.496]
perr = np.sqrt(np.diag(pcov))                              # [0.027 0.030 0.012]
y_pred = f(t, *popt)
r2 = 1 - np.sum((ydata-y_pred)**2) / np.sum((ydata-ydata.mean())**2)
```

### `least_squares` — 残差向量形式的最小二乘（更底层、更灵活）

```python
scipy.optimize.least_squares(fun, x0, jac='2-point', bounds=(-np.inf, np.inf),
    method='trf', ftol=1e-08, xtol=1e-08, gtol=1e-08, x_scale=None,
    loss='linear', f_scale=1.0, diff_step=None, tr_solver=None,
    tr_options=None, jac_sparsity=None, max_nfev=None, verbose=0,
    args=(), kwargs=None, ...)
```

| 参数 | 作用 |
| --- | --- |
| `fun` | 返回**残差向量** `r(x)`（不是标量），例如 `r = y_pred - y` |
| `method` | `'trf'`（默认）/`'dogbox'`/`'lm'`（`lm` 不支持 bounds） |
| `loss` | `'linear'`、`'soft_l1'`、`'huber'`、`'cauchy'`、`'arctan'`——**鲁棒回归**抗离群点，配合 `f_scale` |
| `max_nfev` | 最大函数评估次数 |

**返回值**：`res.x`（解）、`res.cost`（= 0.5·Σr²）、`res.fun`（残差）、`res.jac`、`res.optimality`（一阶最优度，越小越收敛）、`res.success`

```python
res = opt.least_squares(lambda p: f(t, *p) - ydata, x0=[1,1,0],
                        loss='soft_l1', f_scale=0.1)   # 含离群点时用
```

### `root` / `brentq` / `newton` / `fsolve` — 方程求根

```python
scipy.optimize.root(fun, x0, args=(), method='hybr', jac=None, tol=None,
                    callback=None, options=None)          # 方程组（多元）
scipy.optimize.brentq(f, a, b, args=(), xtol=2e-12, rtol=..., maxiter=100,
                      full_output=False, disp=True)       # 一元，区间 [a,b] 上
scipy.optimize.newton(func, x0, fprime=None, args=(), tol=1.48e-08, maxiter=50,
                      fprime2=None, x1=None, rtol=0.0, full_output=False, disp=True)
scipy.optimize.fsolve(func, x0, args=(), fprime=None, full_output=False,
                      col_deriv=False, xtol=..., maxfev=0, ...)
```

| 函数 | 关键点 | 返回值用法 |
| --- | --- | --- |
| `root` | `fun(x)` 返回残差向量；`method='hybr'`（默认）/`'lm'`/`'krylov'`；`tol` 设收敛容差 | `res.x`（根）、`res.fun`（残差，≈0 表示可信）、`res.success` |
| `brentq` | **必须** `f(a)·f(b) < 0`；区间内单根时最稳健 | 直接返回根 `float`；`full_output=True` 时返回 `(root, RootResults)` |
| `newton` | 牛顿/割线法；给 `fprime` 用牛顿更快，不给则割线 | 返回根 `float`（可能不收敛，需检查） |
| `fsolve` | `root(method='hybr')` 的旧包装 | 返回根数组 `ndarray`；`full_output=True` 时 `infodict['fvec']` 为残差 |

```python
r = opt.brentq(lambda x: x**2 - 2, 0, 2)              # √2
res = opt.root(lambda p: [p[0]+p[1]-3, p[0]-p[1]-1], x0=[0,0]); res.x   # [2,1]
```

### `linear_sum_assignment` — 指派问题（匈牙利算法）

```python
scipy.optimize.linear_sum_assignment(cost_matrix, maximize=False)
```

- `cost_matrix`：成本矩阵 `(N, M)`；`maximize=True` 时改为最大化收益

**返回值**：`(row_ind, col_ind)` 最优配对索引数组；总代价 = `cost[row_ind, col_ind].sum()`

```python
cost = np.array([[4,1,3],[2,0,5],[3,2,2]])
r, c = opt.linear_sum_assignment(cost)
cost[r, c].sum()                # 最小总成本 = 5
```

### optimize 其余函数速查表

| 函数签名（缩写） | 作用与返回值 |
| --- | --- |
| `nnls(A, b, *, maxiter=None)` | 非负最小二乘；返回 `(x, rnorm)` 残差范数 |
| `fixed_point(func, x0, args=(), xtol=1e-08, maxiter=500, method='del2')` | 不动点迭代 `x=g(x)`；返回收敛点数组 |
| `dual_annealing(func, bounds, maxiter=1000, ...)` | 模拟退火全局优化；`res.x/.fun/.success` |
| `shgo(func, bounds, constraints=None, n=100, ...)` | 全局+凸结构优化，可找多个极小；`res.x/.fun`，`res.xl` 全部极小点 |
| `NonlinearConstraint(fun, lb, ub, jac='2-point', ...)` | 定义非线性约束 `lb <= fun(x) <= ub`（配 `minimize(method='trust-constr')`） |
| `LinearConstraint(A, lb=-inf, ub=inf)` | 定义线性约束 `lb <= A@x <= ub` |
| `Bounds(lb=-inf, ub=inf)` | 定义变量上下界对象（`milp`/`least_squares`/`trust-constr` 用） |

---

## 1.2 scipy.integrate 积分与微分方程

### `quad` — 一元数值积分（自适应）

```python
scipy.integrate.quad(func, a, b, args=(), full_output=0, epsabs=1.49e-08,
    epsrel=1.49e-08, limit=50, points=None, weight=None, wvar=None,
    wopts=None, maxp1=50, limlst=50, complex_func=False)
```

| 参数 | 作用 |
| --- | --- |
| `func` | 被积函数 `f(x, *args)`：**输入标量，返回标量** |
| `a`, `b` | 积分上下限；**无穷积分直接用 `np.inf` / `-np.inf`** |
| `args` | 传给 `func` 的额外参数 |
| `points` | 奇点/间断点位置列表，如 `points=[1.0]`（仅在有限区间有效） |
| `limit` | 子区间最大数（不好收敛时调大，如 200） |
| `weight` | `'sin'/'cos'/'alg'` 等特殊权函数积分 |
| `complex_func` | 复值被积函数设 `True` |

**返回值**：`(val, abserr)` —— 积分值与绝对误差估计。精度不够时先看 `abserr`，再加大 `limit` 或拆区间。

```python
val, err = integrate.quad(lambda x: np.exp(-x**2), 0, np.inf)   # ≈ √π/2
val, err = integrate.quad(lambda x, k: np.sin(k*x)/x, 0, 1, args=(2,))  # 带参数
```

### `dblquad` / `tplquad` / `nquad` — 多重积分

```python
scipy.integrate.dblquad(func, a, b, gfun, hfun, args=(), epsabs=1.49e-08, epsrel=1.49e-08)
scipy.integrate.tplquad(func, a, b, gfun, hfun, qfun, rfun, args=(), ...)
scipy.integrate.nquad(func, ranges, args=None, opts=None, full_output=False)
```

⚠️ **参数顺序是最大的坑**：
- `dblquad` 计算 `∫ₐᵇ ∫_{gfun(x)}^{hfun(x)} func(y, x) dy dx` —— `func` 的**第一个参数是先积（内层）变量**；`a,b` 是外层变量 x 的范围；`gfun/hfun` 可以是常数或 `lambda x: ...`
- `tplquad`：`func(z, y, x)`，先积 z、再 y、最后 x

**返回值**：`(val, abserr)`

```python
val, err = dblquad(lambda y, x: y*x, 0, 1, lambda x: 0, lambda x: x)  # ∫₀¹∫₀ˣ yx dy dx = 0.5
```

### `solve_ivp` — 常微分方程组（ODE 初值问题，核心函数）

```python
scipy.integrate.solve_ivp(fun, t_span, y0, method='RK45', t_eval=None,
    dense_output=False, events=None, vectorized=False, args=None, **options)
```

| 参数 | 作用 |
| --- | --- |
| `fun` | 右端函数 `fun(t, y)`：`y` 是 ndarray（状态向量），返回 `dy/dt` 同形状数组 |
| `t_span` | `(t0, tf)` 求解区间 |
| `y0` | 初值 ndarray，形状 `(n,)` |
| `method` | `'RK45'`(默认非刚) / `'RK23'` / `'DOP853'`(高精度非刚) / **`'Radau'`、`'BDF'`(刚性问题)** / `'LSODA'`(自动) |
| `t_eval` | 想输出结果的时间点数组（如作图用的 `np.linspace(0,10,200)`） |
| `dense_output` | `True` 时返回连续插值器 `res.sol(t)`，可取任意时刻状态 |
| `events` | 事件函数列表 `g(t,y)=0` 触发（如相图越界、 extinction）；给函数加属性 `g.terminal=True` 停止积分、`g.direction` 限定穿越方向 |
| `args` | 传给 `fun` 的模型参数元组，如 `args=(a, b)` |
| `options` | `rtol=1e-3, atol=1e-6`（精度）、`max_step`（步长上限）、`first_step` |

**返回值** `OdeSolution` 结果对象：
- `res.t`：时间点数组 `(m,)`；`res.y`：状态数组，**形状 `(n, m)`**（每行一个状态变量）
- `res.sol`：`dense_output=True` 时为可调用 `sol(t) → y(t)`
- `res.t_events[i]` / `res.y_events[i]`：事件发生时间/状态
- `res.success`、`res.status`（0=成功，-1=步长失败）、`res.nfev`（函数调用次数）

```python
# dS/dt=-βSI, dI/dt=βSI-γI (SIR 模型)
def sir(t, y, beta, gamma):
    S, I, R = y
    return [-beta*S*I, beta*S*I - gamma*I, gamma*I]
sol = solve_ivp(sir, (0, 100), [0.99, 0.01, 0.0], args=(1.5, 0.3),
                t_eval=np.linspace(0, 100, 200), method='RK45')
plt.plot(sol.t, sol.y.T)          # 每行一个变量 → 转置后逐列画
```

### `odeint` — ODE（旧接口，仍在广泛使用）

```python
scipy.integrate.odeint(func, y0, t, args=(), Dfun=None, col_deriv=0,
    full_output=0, ..., rtol=None, atol=None, ..., tfirst=False)
```

⚠️ 与 `solve_ivp` 的两点区别：`func` 默认签名是 **`func(y, t, ...)`（y 在前！）**；返回值是普通 ndarray，形状 **`(len(t), n)`（每行一个时刻）**。新版本可设 `tfirst=True` 改为 `func(t, y)`。

```python
ys = integrate.odeint(sir_yt, [0.99, 0.01, 0.0], t, args=(1.5, 0.3))  # (200, 3)
```

### `solve_bvp` — ODE 两点边值问题

```python
scipy.integrate.solve_bvp(fun, bc, x, y, p=None, S=None, fun_jac=None,
    bc_jac=None, tol=0.001, max_nodes=1000, verbose=0, bc_tol=None)
```

| 参数 | 作用 |
| --- | --- |
| `fun(x, y)` | 返回 `y'`，形状同 y；**未知参数 p 存在时返回 `[:n], p`** |
| `bc(ya, yb)` | 边界条件残差（左端 ya、右端 yb），返回长度 = 条件数 |
| `x` | 初始网格点数组（单调，如 `np.linspace(0,1,25)`） |
| `y` | 初值猜测，形状 `(n, len(x))` |
| `p` | 未知参数初值（边值问题中反演参数用） |

**返回值**：`res.x`（网格）、`res.y`（解 `(n, m)`）、`res.p`（拟合出的参数）、`res.sol(t)` 连续插值、`res.success`

```python
# y'' + |y| = 0, y(0)=0, y(1)=1
f  = lambda x, y: np.vstack((y[1], -np.abs(y[0])))
bc = lambda ya, yb: np.array([ya[0], yb[0]-1])
x0 = np.linspace(0, 1, 25)
res = solve_bvp(f, bc, x0, np.zeros((2, x0.size))); y = res.sol(np.linspace(0,1,100))[0]
```

### 离散数值积分

```python
scipy.integrate.simpson(y, x=None, *, dx=1.0, axis=-1)     # 辛普森法（精度高）
scipy.integrate.trapezoid(y, x=None, dx=1.0, axis=-1)      # 梯形法
scipy.integrate.cumulative_trapezoid(y, x=None, dx=1.0, axis=-1, initial=None)
```

- `y`：采样值；`x`：采样坐标（或给等间距 `dx`）
- **返回值**：`simpson/trapezoid` 返回标量（定积分）；`cumulative_trapezoid` 返回与 y 等长的前缀积分数组（`initial=0` 使长度对齐，画累积曲线用）

```python
x = np.linspace(0, np.pi, 100)
integrate.simpson(np.sin(x), x=x)                    # ≈ 2
acc = integrate.cumulative_trapezoid(np.abs(np.sin(x)), x, initial=0)
```

---

## 1.3 scipy.interpolate 插值

### `interp1d` — 一维插值（经典接口，返回可调用对象）

```python
scipy.interpolate.interp1d(x, y, kind='linear', axis=-1, copy=True,
                           bounds_error=None, fill_value=np.nan, assume_sorted=False)
```

| 参数 | 作用 |
| --- | --- |
| `x`, `y` | 已知数据点（x 需一维；`assume_sorted=False` 时自动排序） |
| `kind` | `'linear'`(默认) / `'nearest'` / `'zero'` / `'slinear'` / `'quadratic'` / `'cubic'` / `'previous'` / `'next'`，或整数阶 |
| `bounds_error` | 查询超出范围时报错（默认 True，线性为 False） |
| `fill_value` | 区间外的填充值；**`'extrapolate'` 允许外推** |

**返回值**：可调用对象 `f(x_new) → y_new`（也可用于反查/外推）

```python
f = interp1d(xk, yk, kind='cubic', fill_value='extrapolate')
y_new = f(x_dense)
```

> ⚠️ 新版 scipy 已将 `interp1d` 标记为"不建议用于新代码"，新代码优先用 `make_interp_spline` / `CubicSpline`，但建模中仍随处可见。

### `CubicSpline` — 三次样条插值（推荐）

```python
scipy.interpolate.CubicSpline(x, y, axis=0, bc_type='not-a-knot', extrapolate=None)
```

| 参数 | 作用 |
| --- | --- |
| `bc_type` | `'not-a-knot'`(默认) / `'natural'`(二阶导为0) / `'clamped'`(一阶导为0) / `'periodic'`，或显式条件 `((1, v1), (2, v2))`（端点一阶/二阶导数值） |
| `extrapolate` | `True` 外推 / `False` 返回 nan / `'periodic'` |

**返回值**：可调用样条对象，且自带方法：
- `s(x_new)` 求值；`s.derivative(n)` 得 n 阶导数样条；`s.antiderivative()` 原函数
- `s.integrate(a, b)` 样条定积分；`s.roots()` 求零点；`s.c` 样条系数

```python
s = CubicSpline(xk, yk, bc_type='natural')
y_new, dy = s(x_new), s.derivative(1)(x_new)     # 插值 + 导数
```

### `PchipInterpolator` / `Akima1DInterpolator` — 防震荡插值

```python
scipy.interpolate.PchipInterpolator(x, y, axis=0, extrapolate=None)
scipy.interpolate.Akima1DInterpolator(x, y, axis=0, *, method='akima', extrapolate=None)
```

- `Pchip`：**保单调**三次插值，数据有极值时不会产生虚假振荡（画图安全）
- `Akima`：对非光滑数据更稳健；`method='makima'` 为改进版

**返回值**：同 CubicSpline，可调用对象。

### `UnivariateSpline` — 样条拟合/平滑（s 控制光滑度）

```python
scipy.interpolate.UnivariateSpline(x, y, w=None, bbox=[None, None], k=3,
                                   s=None, ext=0, check_finite=False)
```

| 参数 | 作用 |
| --- | --- |
| `k` | 样条阶数（1–5） |
| `s` | 平滑因子：**`s=0` 强制过点（插值）**；`s>0` 为带噪声数据做平滑拟合（噪声大调大 s） |
| `w` | 每点权重（常取 `1/σ`） |
| `ext` | 外推行为：0 外推、1 边界外为 0、2 报错、3 边界常值 |

**返回值**：样条对象，方法同 CubicSpline，另有 `s.get_coeffs()`、`s.set_smoothing_factor(s)`。

### `griddata` — 散点数据插值（多维，核心函数）

```python
scipy.interpolate.griddata(points, values, xi, method='linear',
                           fill_value=np.nan, rescale=False)
```

| 参数 | 作用 |
| --- | --- |
| `points` | 散点坐标：`(N, D)` 数组或 `(x_arr, y_arr)` 元组 |
| `values` | 每个散点处的值，`(N,)` |
| `xi` | 查询点：`(M, D)` 或 meshgrid 的 `(X, Y)` 元组 |
| `method` | `'nearest'` / `'linear'`(默认，基于 Delaunay) / `'cubic'`（仅 1D 和 2D） |
| `rescale` | 各维尺度差异大时设 True（先归一化再插值） |
| `fill_value` | 凸包外填的值（默认 nan） |

**返回值**：查询点处的插值数组。

```python
zi = griddata((lon, lat), temp, (XI, YI), method='cubic')   # 生成规则网格用于绘图
```

### `RegularGridInterpolator` / `interpn` — 规则网格插值（快）

```python
scipy.interpolate.RegularGridInterpolator(points, values, method='linear',
    bounds_error=True, fill_value=np.nan)
scipy.interpolate.interpn(points, values, xi, method='linear',
    bounds_error=True, fill_value=np.nan)
```

- `points`：**每维一维坐标数组组成的列表** `[xs, ys]`（不必等间距）
- `values`：网格值，`values[i,j]` 对应 `(xs[i], ys[j])`
- `method`：`'linear'` / `'nearest'` / `'slinear'` / `'cubic'` / `'quintic'` / `'pchip'`
- **返回值**：查询点 `(M, D)` 处的插值数组（`RegularGridInterpolator` 是可调用对象）

### 其余插值函数速查表

| 函数签名（缩写） | 作用与返回值 |
| --- | --- |
| `lagrange(x, w)` | 拉格朗日插值；返回 `numpy.poly1d`（高次易震荡，慎用） |
| `barycentric_interpolate(xi, yi, x, axis=0)` | 重心插值（数值稳定的高次多项式）；返回插值数组 |
| `make_interp_spline(x, y, k=3, t=None, bc_type=None, axis=0)` | 官方推荐的 B 样条插值；返回 BSpline（可调用，`s.derivative()`） |
| `RBFInterpolator(y, d, neighbors=None, smoothing=0.0, kernel='thin_plate_spline', epsilon=None, degree=None)` | 高维散点径向基插值（griddata 的强大替代）；可调用对象 |
| `Rbf(*coords, function='multiquadric', smooth=0, ...)` | 旧版径向基；返回可调用对象（新代码用 RBFInterpolator） |

---

## 1.4 scipy.stats 统计检验与概率分布

### 概率分布统一接口（以 `norm` 为例）

```python
from scipy import stats
d = stats.norm(loc=0, scale=1)     # 任意分布同构：t(df), chi2(df), f(dfn,dfd), uniform(loc,scale),
                                   # expon(loc,scale), lognorm(s), beta(a,b), gamma(a), weibull_min(c),
                                   # binom(n,p), poisson(mu) ...
```

| 方法 | 作用 | 返回值 |
| --- | --- | --- |
| `d.pdf(x)` / `d.pmf(k)` | 概率密度 / 分布律 | 数组 |
| `d.cdf(x)` | 累积分布 | 数组 |
| `d.ppf(q)` | 分位数（逆 CDF），临界值查表用它 | 数组 |
| `d.sf(x)` / `d.isf(q)` | 生存函数 1-CDF / 逆生存函数 | 数组 |
| `d.rvs(size=1000, random_state=seed)` | 随机采样 | ndarray |
| `d.fit(data)` | MLE 参数估计（连续分布） | 参数元组，如 norm → `(mu, sigma)` |
| `d.interval(0.95, loc, scale)` | 置信区间 | `(lo, hi)` |
| `d.mean/var/std/expect` | 数字特征 | 标量 |

```python
mu, sigma = stats.norm.fit(data)                      # 正态拟合
lo, hi = stats.norm.interval(0.95, loc=xbar, scale=sem)   # 均值 95% 置信区间
z = stats.t.ppf(0.975, df=n-1)                        # t 临界值
```

> 注意：`scale` 是**标准差**不是方差。抽样建议都带 `random_state` 保证可复现。

### `describe` — 描述统计

```python
scipy.stats.describe(a, axis=0, ddof=1, bias=True, nan_policy='propagate')
```

**返回值** `DescribeResult`：`.nobs .minmax .mean .variance .skewness .kurtosis`

```python
d = stats.describe(x); print(d.mean, d.variance, d.skewness)
```

### 假设检验三件套（t 检验）

```python
scipy.stats.ttest_1samp(a, popmean, axis=0, nan_policy='propagate', alternative='two-sided')
scipy.stats.ttest_ind(a, b, *, axis=0, equal_var=True, alternative='two-sided')
scipy.stats.ttest_rel(a, b, axis=0, alternative='two-sided')     # 配对样本
```

- `popmean`：假设的总体均值
- `equal_var`：两总体方差是否相同；**不确定时设 `False`（Welch t，更稳健）**
- `alternative`：`'two-sided'` / `'less'` / `'greater'`（注意是备择假设方向）

**返回值** `TtestResult`：`.statistic`（t 值）、`.pvalue`（p 值）——可直接元组解包 `(t, p) = ttest_ind(...)`

```python
t, p = stats.ttest_ind(g1, g2, equal_var=False)
p < 0.05        # 拒绝"均值相同"的原假设
```

### 正态性检验

```python
scipy.stats.shapiro(x, *, axis=None, nan_policy='propagate')      # W 检验（小样本首选，n≤5000）
scipy.stats.normaltest(a, axis=0)                                  # D'Agostino（大样本）
scipy.stats.kstest(rvs, cdf, args=(), alternative='two-sided')     # KS 检验（可对任意分布）
scipy.stats.anderson(x, dist='norm')                               # Anderson-Darling
```

- `kstest` 的 `cdf` 可传 `'norm'` 等分布名 + `args=(mu, sigma)`
- **返回值**：`shapiro/normaltest/kstest` 返回含 `.statistic .pvalue` 的对象（可解包）；`anderson` 返回 `(统计量, critical_values, significance_level)` —— 统计量小于某显著性水平的临界值则不拒绝该水平下的正态性

```python
W, p = stats.shapiro(x)                     # p>0.05 视为近似正态
ks, p = stats.kstest(x, 'norm', args=(x.mean(), x.std()))
```

### 方差齐性检验

```python
scipy.stats.levene(*samples, center='median', proportiontocut=0.05)   # 稳健（推荐）
scipy.stats.bartlett(*samples)                                        # 对非正态敏感
```

**返回值**：`(statistic, pvalue)`；p<0.05 说明方差不齐 → `ttest_ind` 用 `equal_var=False`

### 方差分析（ANOVA）与非参数替代

```python
scipy.stats.f_oneway(*samples, axis=0, equal_var=True)     # 单因素方差分析
scipy.stats.kruskal(*samples)                              # 非参数（任意分布组间比较）
scipy.stats.friedmanchisquare(*samples)                    # 非参数重复测量（配对多组）
```

**返回值**：`(F, p)` / `(H, p)`；p<0.05 → 组间存在显著差异

```python
F, p = stats.f_oneway(g1, g2, g3)
```

### `chi2_contingency` — 列联表卡方独立性检验

```python
scipy.stats.chi2_contingency(observed, correction=True, lambda_=None)
```

- `observed`：二维列联表（频数矩阵，如 2×2）
- `correction`：仅 2×2 时是否用 Yates 连续性校正

**返回值** `Chi2ContingencyResult`（可解包 4 个值）：
`chi2, p, dof, expected = chi2_contingency(tab)` —— `expected` 为期望频数矩阵（各格期望≥5 才有效）

```python
chi2, p, dof, expected = stats.chi2_contingency(tab)
```

### 相关性分析

```python
scipy.stats.pearsonr(x, y, *, alternative='two-sided')    # 皮尔逊（线性相关，要求近似正态）
scipy.stats.spearmanr(a, b=None, axis=0)                  # 斯皮尔曼（秩相关，抗离群、非线性单调）
scipy.stats.kendalltau(x, y, *, variant='b')              # 肯德尔 τ（有序数据）
```

**返回值**：`PearsonRResult` 等对象，含 `.statistic`（相关系数）、`.pvalue`（可解包）；`spearmanr` 传二维矩阵时返回相关系数矩阵 + p 矩阵

```python
r, p = stats.pearsonr(x, y)
rho, p = stats.spearmanr(x, y)
```

### `linregress` — 一元线性回归

```python
scipy.stats.linregress(x, y, alternative='two-sided', *, axis=0)
```

**返回值** `LinregressResult`：`.slope`（斜率）、`.intercept`（截距）、`.rvalue`（相关系数）、`.pvalue`（斜率显著性）、`.stderr` / `.intercept_stderr`（标准误）

```python
reg = stats.linregress(x, y)
y_hat = reg.slope * x + reg.intercept
print(f"y = {reg.slope:.4f}x + {reg.intercept:.4f}, R² = {reg.rvalue**2:.4f}")
```

### 数据预处理与变换

```python
scipy.stats.zscore(a, axis=0, ddof=0, nan_policy='propagate')   # 标准化 (x-μ)/σ
scipy.stats.boxcox(x, lmbda=None, alpha=None)                   # Box-Cox 正态化变换（x>0）
scipy.stats.rankdata(a, method='average')                       # 排名（method: min/max/dense/ordinal）
scipy.stats.mode(a, axis=0, keepdims=False)                     # 众数 → (mode, count)
scipy.stats.sem(a, axis=0, ddof=1)                              # 标准误 σ/√n
scipy.stats.skew(a, axis=0, bias=True)                          # 偏度
scipy.stats.kurtosis(a, axis=0, fisher=True, bias=True)         # 峰度（fisher=True：正态=0）
scipy.stats.binomtest(k, n, p=0.5, alternative='two-sided')     # 二项检验 → .pvalue
scipy.stats.fisher_exact(table, alternative=None)               # 2×2 精确检验 → (odds ratio, p)
scipy.stats.theilslopes(y, x=None, alpha=0.95)                  # 稳健回归斜率 → (slope, intercept, lo, hi)
```

```python
xt, lam = stats.boxcox(x)          # lam 为自动估计的变换指数
z = stats.zscore(x, ddof=1)        # 标准化，论文常见写法
```

> 多元分布：`stats.multivariate_normal(mean, cov)` → `.pdf(X) .logpdf(X) .rvs(size, random_state)`

---

## 1.5 scipy.linalg 线性代数

### `solve` — 线性方程组 Ax=b（不要用 inv！）

```python
scipy.linalg.solve(a, b, lower=False, overwrite_a=False, overwrite_b=False,
                   check_finite=True, assume_a=None, transposed=False)
```

- `a`: `(M, M)` 系数矩阵；`b`: `(M,)` 或 `(M, K)` 多右端
- `assume_a`：`'gen'`(默认)/`'sym'`/`'her'`/`'pos'`（对称/正定矩阵利用结构更快更稳）
- **返回值**：解 `x`，`a@x ≈ b`；直接 `linalg.solve(A, b)`

```python
x = linalg.solve(A, b)                        # 多列 b 时每列是一个解
```

### `eig` / `eigh` — 特征值分解（综合评价、主成分、图中心度）

```python
scipy.linalg.eig(a, b=None, left=False, right=True)     # 一般矩阵
scipy.linalg.eigh(a, b=None, *, lower=True, eigvals_only=False)   # 实对称/Hermitian（推荐更快）
```

**返回值**：`(w, v)` —— `w` 特征值数组，`v` 特征向量**按列**：第 i 个特征向量 = `v[:, i]`。
`eigh` 的特征值**升序**且为实数。

```python
w, v = linalg.eigh(A)                # 对称矩阵
main_pc = v[:, -1]                   # 最大特征值对应的特征向量（PCA 第一主成分）
```

### `svd` — 奇异值分解（降维、伪逆、矩阵秩、数据压缩）

```python
scipy.linalg.svd(a, full_matrices=True, compute_uv=True, overwrite_a=False,
                 check_finite=True, lapack_driver='gesdd')
```

- `full_matrices=False`：返回紧凑型（U `(M,K)`），分解大矩阵时省内存

**返回值**：`(U, s, Vh)` —— `s` 为奇异值**一维数组**（降序），满足 `A = U @ diag(s) @ Vh`。

```python
U, s, Vh = linalg.svd(X, full_matrices=False)
X_rank_k = U[:, :k] @ np.diag(s[:k]) @ Vh[:k]        # 截断 SVD 重建（去噪/压缩）
cond = s[0] / s[-1]                                  # 条件数
V = Vh[:k].T                                         # 前 k 个主成分载荷
```

### `lstsq` — 最小二乘解

```python
scipy.linalg.lstsq(a, b, cond=None, overwrite_a=False, overwrite_b=False,
                   check_finite=True, lapack_driver=None)
```

**返回值**：`(x, residuals, rank, s)` —— `x` 最小二乘解；`residuals` 残差平方和（满秩时）；`rank` 有效秩；`s` 奇异值。超定方程（数据点>参数）求经验模型用它。

```python
A = np.vstack([x, np.ones_like(x)]).T
coef, res, rank, sv = linalg.lstsq(A, y)             # y = coef[0]*x + coef[1]
```

### 分解三件套

```python
scipy.linalg.lu(a, permute_l=False, overwrite_a=False, check_finite=True, p_indices=False)
scipy.linalg.qr(a, overwrite_a=False, mode='full', pivoting=False, check_finite=True)
scipy.linalg.cholesky(a, lower=False, overwrite_a=False, check_finite=True)
```

- `lu`：`permute_l=False`（默认）返回 `(P, L, U)`；`True` 时返回 `(PL, U)`
- `qr`：`mode='full'/'r'/'economic'/'complete'`；返回 `(Q, R)`，Q 正交、R 上三角
- `cholesky`：⚠️ **默认返回上三角 R**（`L = cholesky(A, lower=True)` 才是下三角）；要求 A 对称正定

```python
P, L, U = linalg.lu(A)
Q, R = linalg.qr(A)
R = linalg.cholesky(A)                # A = R.T @ R
```

### 其余线性代数速查表

| 函数签名（缩写） | 作用与返回值 |
| --- | --- |
| `inv(a)` | 逆矩阵（**解方程请用 solve，不要 inv**） |
| `det(a)` | 行列式（判断可逆/体积缩放） |
| `pinv(a, *, atol=None, rtol=None, return_rank=False)` | Moore-Penrose 伪逆（病态/不满秩问题） |
| `norm(a, ord=None, axis=None, keepdims=False)` | 范数；矩阵 ord 可取 1/2/inf/`'fro'`；向量可取 0/1/2/inf/-1/-2 |
| `null_space(A, rcond=None)` | 零空间标准正交基，**列为基向量**（解齐次方程 Ax=0） |
| `expm(A)` | 矩阵指数 eᴬ（连续马尔可夫转移、矩阵微分方程） |
| `sqrtm(A)` / `schur(a, output='real')` / `block_diag(*arrs)` | 矩阵平方根 / Schur 分解 `(T, Z)` / 分块对角拼接 |

```python
Q = np.array([[-0.5, 0.5], [0.5, -0.5]])    # 连续马尔可夫转移率矩阵
P1 = linalg.expm(Q * 1.0)                   # P(t) = expm(Q·t)：t=1 时刻的状态转移矩阵
```

---

## 1.6 scipy.sparse 稀疏矩阵

大规模（节点/变量数 > 数千）规划、图模型、有限元必备，稠密矩阵会爆内存。

| 用法 | 说明 |
| --- | --- |
| `sparse.csr_matrix((data, (row, col)), shape=(m, n))` | COO 三元组直接建（**建图/建约束推荐**，自动合并重复项） |
| `sparse.csr_matrix(dense_array)` / `.toarray()` | 稠密 ↔ 稀疏互转 |
| `M @ v` / `M.T` / `M + M2` | 运算符与稠密一致；**矩阵乘法用 `@`** |
| `M.tocoo() / .tocsc() / .tolil()` | 格式互转：COO 快速构造、CSC 列操作、LIL 逐元素赋值 |
| `M.nnz` / `M.shape` | 非零元个数 / 形状 |
| `sparse.eye(n, format='csr')` / `sparse.hstack([...]) / vstack / bmat` | 单位阵 / 拼接（都支持 `format=` 指定格式） |
| `sparse.save_npz('m.npz', M)` / `sparse.load_npz('m.npz')` | 持久化 |

```python
A = sparse.csr_matrix((np.ones(len(edges)), (src, dst)), shape=(n, n))
deg = np.asarray(A.sum(axis=1)).ravel()      # ⚠️ 稀疏 sum 得到二维矩阵 → ravel
res = opt.linprog(c, A_ub=A, b_ub=b)         # linprog/minimize 都接受稀疏矩阵
```

---

## 1.7 scipy.cluster 聚类

### 层次聚类三步曲

```python
Z = sch.linkage(y, method='single', metric='euclidean', optimal_ordering=False)
labels = sch.fcluster(Z, t, criterion='inconsistent', depth=2)
sch.dendrogram(Z, p=30, truncate_mode=None, color_threshold=None, labels=None,
               orientation='top', no_plot=False, show_leaf_counts=True)
```

| 参数 | 作用 |
| --- | --- |
| `linkage.y` | 观测矩阵 `(m, n)`，或 `pdist` 得到的压缩距离向量 |
| `method` | `'single'`(最近邻) / `'complete'` / `'average'` / `'weighted'` / `'ward'`(方差最小，最常用) / `'centroid'` / `'median'`（后三者仅欧氏距离） |
| `metric` | 样本间距离（ward 等仅支持欧氏） |
| `fcluster.t` | 阈值：`criterion='distance'` 时为合并距离；`criterion='maxclust'` 时为**簇数上限**（最常用！） |
| `dendrogram.p` / `truncate_mode` | `truncate_mode='lastp', p=20` 压缩显示尾部，大图必用 |

**返回值**：`Z` 形状 `(n-1, 4)` 合并记录；`fcluster` 返回每个样本的簇标签（1..k）；`dendrogram` 返回绘图数据字典（配合 `plt.show()`）

```python
Z = sch.linkage(X, method='ward')
labels = sch.fcluster(Z, t=3, criterion='maxclust')
sch.dendrogram(Z, truncate_mode='lastp', p=20); plt.show()
```

### K-means（scipy 版）与向量化

```python
centroids, labels = scipy.cluster.vq.kmeans2(data, k, iter=10, minit='++', seed=None)
code, dist = scipy.cluster.vq.vq(obs, code_book)
```

- `minit='++'`：k-means++ 初始化（比默认 `'random'` 稳）
- **返回值**：`kmeans2` → `(质心 (k,n), 标签 (m,))`；`vq` → 每个新样本归属的簇号与距离

> 更常用 `sklearn.cluster.KMeans`；scipy 版胜在轻量无依赖。

---

## 1.8 scipy.spatial 空间计算

### 距离矩阵

```python
D = scipy.spatial.distance.cdist(XA, XB, metric='euclidean', **kwargs)  # (M, N)
condensed = scipy.spatial.distance.pdist(X, metric='euclidean')          # (M(M-1)/2,)
D2 = scipy.spatial.distance.squareform(condensed)                        # 压缩↔方阵 互转
```

- `metric`：`'euclidean' / 'cityblock'(曼哈顿) / 'chebyshev' / 'minkowski'(配 p=) / 'cosine' / 'correlation' / 'hamming' / 'jaccard' / 'sqeuclidean'` 或自定义函数
- ⚠️ `squareform`：方阵→压缩向量会去对角线；`linkage` 可直接吃 `pdist` 的输出
- 新版已移除 `spatial.distance.distance_matrix`，一律用 `cdist`

```python
D = cdist(centers, points, metric='minkowski', p=1)    # 到各中心点曼哈顿距离
nearest = D.argmin(axis=0)                             # 最近中心索引（最近邻分类）
```

### `cKDTree` — K 近邻 / 邻域查询（大数据下比 cdist 快得多）

```python
tree = scipy.spatial.cKDTree(data, leafsize=16)
dist, idx = tree.query(x, k=1, p=2, distance_upper_bound=np.inf, workers=1)
```

- `query.x`：查询点（可一批）；`k`：邻居个数（`k=2` 返回最近+次近）
- **返回值**：`(distances, indices)`，与查询点同形
- 其他方法：`tree.query_ball_point(x, r)` 半径 r 内所有点；`tree.query_pairs(r)` 点对

```python
dist, idx = tree.query(grid_points, k=3)     # 每个格点的 3 近邻距离与索引
```

### 计算几何

```python
from scipy.spatial import ConvexHull, Delaunay, Voronoi
hull = ConvexHull(points)            # .vertices(顶点索引, 逆时针) .volume .area .equations
tri  = Delaunay(points)              # .simplices(三角形顶点索引) .find_simplex(pt)
vor  = Voronoi(points)               # .vertices .ridge_vertices .regions
```

⚠️ **二维的坑**：`hull.volume` 是**面积**，`hull.area` 是**周长**。

```python
area = hull.volume                                   # 凸包面积
inside = tri.find_simplex(query_pts) >= 0            # 点是否在凸包/三角网内
hull_path = points[hull.vertices]                    # 逆时针取凸包顶点用于画图
```

---

## 1.9 scipy.signal 数据平滑与信号

### `savgol_filter` — Savitzky-Golay 平滑（实验数据平滑首选）

```python
scipy.signal.savgol_filter(x, window_length, polyorder, deriv=0, delta=1.0,
                           axis=-1, mode='interp', cval=0.0)
```

- `window_length`：窗口长度，**必须为奇数**且 > `polyorder`
- `polyorder`：拟合多项式阶数（3~5 常用）
- `deriv`：>0 时输出的是平滑后的 `deriv` 阶导数（数值微分去噪一步完成）

**返回值**：与 x 等长的平滑数组

```python
y_s = signal.savgol_filter(y, window_length=11, polyorder=3)
dy  = signal.savgol_filter(y, 11, 3, deriv=1, delta=dt)   # 去噪后的一阶导
```

### `find_peaks` — 峰值检测

```python
peaks, props = scipy.signal.find_peaks(x, height=None, threshold=None,
    distance=None, prominence=None, width=None, wlen=None, rel_height=0.5,
    plateau_size=None)
```

| 条件参数 | 含义 |
| --- | --- |
| `height` | 峰高阈值，可为标量或 `(min, max)` |
| `distance` | 相邻峰最小水平距离（抑制碎峰，最常用） |
| `prominence` | 峰的突出度，`(min, max)`；过滤小起伏的关键 |
| `width` | 半高宽阈值；`rel_height`：相对高度比例 |

**返回值**：`(peaks, properties)` —— `peaks` 为峰位置索引数组；`properties['peak_heights'] / ['prominences'] / ['widths'] / ['left_bases']` 为对应属性

```python
peaks, props = signal.find_peaks(y, height=0.5, distance=10, prominence=0.3)
plt.plot(x, y); plt.plot(x[peaks], y[peaks], 'ro')
```

### 频谱与包络

```python
f, Pxx = scipy.signal.periodogram(x, fs=1.0, window='boxcar', detrend='constant',
                                  scaling='density')             # 整段频谱（方差小偏差小）
f, Pxx = scipy.signal.welch(x, fs=1.0, window='hann_periodic', nperseg=None,
                            noverlap=None, scaling='density')    # 分段平均（方差小，推荐）
z = scipy.signal.hilbert(x)                                      # 解析信号
```

- `fs`：采样频率；`welch.nperseg`：每段长度（频率分辨率 = fs/nperseg）
- **返回值**：`(频率数组 f, 功率谱密度 Pxx)`；主频 = `f[Pxx.argmax()]`
- `hilbert` 返回复解析信号：**包络 = `np.abs(z)`，瞬时相位 = `np.angle(z)`**

```python
f, Pxx = signal.welch(y, fs=24, nperseg=256)
period = 1 / f[Pxx.argmax()]                       # 主周期
env = np.abs(signal.hilbert(y))                    # 包络线
```

### 其余 signal 速查表

| 函数签名（缩写） | 作用与返回值 |
| --- | --- |
| `medfilt(volume, kernel_size=None)` | 中值滤波去椒盐噪声；返回同形数组 |
| `detrend(data, axis=-1, type='linear', bp=0)` | 去趋势（`'linear'`/`'constant'`）；返回同形数组 |
| `resample(x, num, t=None, axis=0, window=None)` | FFT 重采样到 num 点；返回新数组 |
| `resample_poly(x, up, down, axis=0, window=('kaiser', 5.0))` | 多相滤波重采样（如 8kHz→16kHz：`up=2, down=1`） |
| `butter(N, Wn, btype='low', analog=False, output='ba', fs=None)` | 设计 Butterworth 滤波器；返回 `(b, a)` 系数 |
| `filtfilt(b, a, x, axis=-1, padtype='odd')` | 零相位前后向滤波（不引入相位延迟）；返回滤波后数组 |
| `argrelmax(data, axis=0, order=1, mode='clip')` | 局部极大值索引（简单版找峰） |
| `correlate(in1, in2, mode='full', method='auto')` | 互相关（时延估计：峰位即延迟） |

```python
b, a = signal.butter(4, 0.1, btype='low')        # 4 阶低通，截止频率 0.1×fs/2
y_f = signal.filtfilt(b, a, y)                   # 零相位滤波
```

---

# 二、SymPy 符号计算

> 用途：推导公式（求导/积分/解方程）、得出解析解后再 `lambdify` 转数值、生成论文用的 LaTeX 公式。

## 2.1 符号定义与常量

```python
sp.symbols(names, *, cls=sp.Symbol, **assumptions)
sp.Rational(p, q=None, gcd=None)
```

| 参数 | 作用 |
| --- | --- |
| `names` | 空格分隔的字符串 `'x y z'`；范围写法 `'x0:5'` → `(x0,...,x4)`；也可传元组 |
| `assumptions` | 假设条件，**会直接影响化简/求解结果**：`real=True, positive=True, integer=True, negative=True, nonzero=True, nonnegative=True, bool=True` |

```python
x, y = sp.symbols('x y', real=True)      # 建模必给 real/positive，否则 sqrt(x**2)≠x 等
t = sp.symbols('t', positive=True)
a = sp.Rational(1, 3)                    # 精确分数 1/3（区别于 0.333...）
```

**常用常量与取值**：

| 符号 | 含义 | 常用操作 |
| --- | --- | --- |
| `sp.pi, sp.E, sp.I` | π、e、虚数单位 | 参与表达式运算保持精确 |
| `sp.oo` | 正无穷（积分限、极限） | `integrate(f, (x, 0, sp.oo))` |
| `sp.Float(0.1, 30)` | 30 位精度小数 | 高精度计算 |
| `expr.subs({x: 2, y: 3})` | 代入数值/表达式 | 返回新表达式 |
| `sp.N(expr, 20)` / `expr.evalf(20)` | 数值求值到 n 位 | `.evalf(6, subs={x: 1.5})` |
| `sp.nsimplify(0.3333, rational=True)` | 把浮点数识别为精确分数/常数 | `nsimplify(3.14159, [sp.pi])` → π |
| `sp.init_printing()` | Jupyter 里美观打印 | 一次性设置 |

## 2.2 化简与展开

| 函数签名（缩写） | 作用 | 示例 → 结果 |
| --- | --- | --- |
| `simplify(expr, ratio=1.7, rational=False)` | 通用化简（自动选择策略） | `simplify(sin(x)**2 + cos(x)**2)` → 1 |
| `expand(e, deep=True, mul=True, log=True, power_exp=True, **hints)` | 展开 | `expand((x+y)**2)` → x²+2xy+y²；`expand(log(x*y), force=True)` → log x + log y |
| `factor(f, *gens, deep=False)` | 因式分解 | `factor(x**2-x-6)` → (x-3)(x+2) |
| `collect(expr, syms, func=None, exact=False)` | 按指定符号幂次合并 | `collect(x**2+x*y, x)` → x(x+y)+x² 项合并 |
| `cancel(f, *gens)` | 有理式约分 | `cancel((x**2+2*x+1)/(x**2+x))` |
| `apart(f, x=None, full=False)` | 部分分式分解（积分前处理） | `apart(1/((x+1)(x+2)), x)` |
| `together(expr, deep=False, fraction=True)` | 通分合并 | apart 的逆操作 |
| `trigsimp(expr, inverse=False)` | 三角化简 | `trigsimp(sin/cos/tan 混合式)` |
| `expand_trig(expr, deep=True)` | 三角展开 | `expand_trig(sin(x+y))` → sin x cos y + cos x sin y |
| `radsimp(expr, symbolic=True, max_terms=4)` | 分母有理化 | |
| `nsimplify(expr, constants=(), tolerance=None, rational=None)` | 识别常数/化简数值 | 见 2.1 |

```python
sp.simplify(sp.sin(x)**2/sp.cos(x)**2 + sp.cos(x)**2/sp.sin(x)**2)   # → 1/(sin²cos²) 类
sp.collect(sp.expand((x+y)*(x+z)), x)
```

## 2.3 解方程

### `solve` — 万能求解入口

```python
sp.solve(f, *symbols, **flags)      # flags: dict=True, set=True, rational=False, check=True
```

| 参数 | 作用 |
| --- | --- |
| `f` | 表达式（默认=0）/ `Eq(l, r)` / 方程列表（方程组） |
| `*symbols` | 要求解的变量；方程组时传列表 `solve([...], [x, y])` |
| `dict=True` | 返回 `[{x: 值, ...}, ...]`（便于 `.subs()` 链式使用，推荐） |
| `rational=False` | 遇到浮点系数不转分数（**含 float 数据时务必加，否则可能卡死**） |
| `check` | 是否验根（无解方程返回空列表 `[]`） |

**返回值**：单变量 → 解列表；多变量 dict 模式 → 字典列表；线性方程组 → 参解元组

```python
sp.solve(x**2 - 5*x + 6, x)                              # [2, 3]
sp.solve([x + y - 3, x - y - 1], [x, y], dict=True)      # [{x: 2, y: 1}]
sp.solve(sp.Eq(2*x + 1, 7), x)                           # [3]
```

### 专项求解器

```python
sp.linsolve(system, *symbols)              # 线性方程组；system=方程列表 或 (A, b)
sp.nonlinsolve(system, *symbols)           # 多项式非线性方程组
sp.solveset(f, symbol=None, domain=sp.S.Complexes)   # 集合式求解（可指定实数域）
sp.nsolve(f, [args,] x0, dict=False, **kwargs)       # 数值解（初值 x0 敏感）
sp.solve_univariate_inequality(expr, gen, relational=True, domain=sp.S.Reals)  # 一元不等式
sp.maximum(f, symbol, domain=sp.S.Reals)   # 最大值（minimum 同理）
```

- `linsolve` **返回值**：解集 `FiniteSet((x, y, ...))`，含自由参数时如 `{(t, 3-2t)}`；无解 `EmptySet`
- `solveset` **返回值**：`FiniteSet` / `Interval` / `ImageSet` / `ConditionSet`（可 `.contains(v)` 判断）
- `nsolve` 单变量 `nsolve(expr, x0)`，方程组 `nsolve((f1, f2), (x, y), (x0, y0))`，返回数值解
- `maximum` **返回值**：最大值（若符号能确定）；`sp.maximum(f, x).evalf()` 转数值；配合 `sp.calculus.util.continuous_domain` 找定义域

```python
sp.linsolve([x+y-3, x-y-1], (x, y))                       # {(2, 1)}
sp.solveset(x**2 - 2, x, domain=sp.S.Reals)               # {-√2, √2}
sp.solve_univariate_inequality(x**2 > 4, x)               # x < -2 ∨ x > 2
sp.maximum(x*(4-x), x)                                    # 4
```

### `dsolve` — 常微分方程解析解

```python
sp.dsolve(eq, func=None, hint='default', simplify=True, ics=None)
sp.classify_ode(eq, func=None, dict=False)                # 查看方程支持哪些解法
```

| 参数 | 作用 |
| --- | --- |
| `eq` | `Eq(f(x).diff(x), k*f(x))` 或列表（方程组） |
| `ics` | 初/边值条件字典：一阶 `{f(0): 1}`；二阶再加 `{f(x).diff(x).subs(x, 0): 0}` |
| `hint` | `'separable' / 'linear' / 'exact' / 'homogeneous' / 'Bernoulli' / 'nth_linear_constant_coeff_homogeneous'` 等；`classify_ode` 查看可用项 |

**返回值**：`Eq(f(x), 解式)`（多个解时为列表）——**解析解取 `.rhs`**

```python
f = sp.Function('f')
sol = sp.dsolve(sp.Eq(f(x).diff(x), 2*f(x)), f(x), ics={f(0): 3})
sol.rhs                    # 3*exp(2*x)
y = sp.lambdify(x, sol.rhs, 'numpy')(np.linspace(0, 2, 100))   # 转数值作图
```

### `rsolve` — 递推数列通项（离散模型）

```python
sp.rsolve(f, y, init=None)     # f=Eq(y(n+1), a*y(n)+...), y=Function('y')(n), init={y(0): 1}
```

```python
n = sp.symbols('n', integer=True)
yy = sp.Function('y')
sp.rsolve(sp.Eq(yy(n+1), 2*yy(n) + 1), yy(n), {yy(0): 0})    # 2**n - 1
```

## 2.4 微积分

### `limit` — 极限

```python
sp.limit(e, z, z0, dir='+')
```

- `dir`：`'+'` 右极限、`'-'` 左极限、`'+-'` 双侧；`z0` 可为 `sp.oo`

```python
sp.limit(sp.sin(x)/x, x, 0)                       # 1
sp.limit((1 + 1/n)**n, n, sp.oo)                  # E
```

### `diff` — 求导/偏导

```python
sp.diff(f, *symbols, **kwargs)
```

- `diff(f, x)` 一阶；`diff(f, x, 2)` 或 `diff(f, x, x)` 二阶；`diff(f, x, y)` 混合偏导
- 不求值形式 `sp.Derivative(f, x)`，之后 `.doit()` 执行（构建大公式时有用）

```python
fxy = x**2 * sp.sin(y)
sp.diff(fxy, x, y)                                # ∂²f/∂x∂y
d = sp.diff(fxy, x); d.subs({x: 1, y: 0})         # 求导后代入数值
```

### `integrate` — 不定积分/定积分/多重积分

```python
sp.integrate(*args, meijerg=None, conds='piecewise', risch=None, manual=None)
```

| 用法 | 含义 |
| --- | --- |
| `integrate(f, x)` | 不定积分 |
| `integrate(f, (x, a, b))` | 定积分（限可为 ±`sp.oo`） |
| `integrate(f, (y, 0, x), (x, 0, 1))` | **多重积分：从左到右依次积出（左边的先积=内层），后面积分的限可含前面的变量** |

⚠️ 积不出来时返回未求值的 `Integral`（积分表无解/超时），可尝试 `f.simplify()` 后再积、加假设（`positive=True`）、或 `manual=True`/`meijerg=True` 换算法。

```python
sp.integrate(x**2, x)                                     # x³/3
sp.integrate(sp.exp(-x**2), (x, 0, sp.oo))                # √π/2
sp.integrate(x*y, (y, 0, x), (x, 0, 1))                   # 1/8  (∫₀¹∫₀ˣ xy dy dx)
```

### `series` / `summation` / `product`

```python
sp.series(expr, x=None, x0=0, n=6, dir='+')      # 泰勒展开（到 n-1 阶，含 O 项）
sp.summation(f, (i, a, b))                       # 级数求和 Σ
sp.product(f, (i, a, b))                         # 连乘 Π
```

- `series` **返回值**：含 `O(x**n)` 的展开式；`.removeO()` 去掉高阶项后可 `lambdify`
- `summation` 无闭式解时返回未求值对象

```python
sp.series(sp.sin(x), x, 0, 5)                     # x - x³/6 + O(x⁵)
sp.series(sp.exp(x), x, 0, 5).removeO()
sp.summation(1/sp.factorial(k), (k, 0, sp.oo))    # E
```

## 2.5 矩阵

### 构造

```python
M = sp.Matrix([[1, 2], [3, 4]])      # 或 Matrix([1,2,3]) 列向量
sp.eye(n); sp.zeros(m, n); sp.ones(m, n); sp.diag(a, b, c)
sp.MatrixSymbol('A', n, m)           # 大矩阵符号（不展开元素）
```

### 常用方法（均返回新对象/值）

| 方法 | 返回值 |
| --- | --- |
| `M.T` / `M**n` / `M**-1` | 转置 / 幂 / 逆 |
| `M.det()` / `M.rank()` | 行列式 / 秩 |
| `M.inv()` | 逆矩阵 |
| `M.rref()` | `(简化行阶梯阵, 主元列索引)` —— 解方程组、判解结构 |
| `M.eigenvals()` | `{特征值: 代数重数}` |
| `M.eigenvects()` | `[(特征值, 重数, [特征向量...]), ...]` |
| `M.diagonalize()` | `(P, D)`：`M = P·D·P⁻¹`（可对角化时） |
| `M.nullspace()` | 零空间基向量列表（Ax=0 解空间） |
| `M.columnspace()` / `M.rowspace()` | 列空间/行空间基 |
| `M.LUsolve(b)` / `M.solve(b)` | 解线性方程组 Mx=b |
| `M.jacobian([x, y, ...])` | 雅可比矩阵（多元函数向量求偏导） |
| `M.norm()` / `M.subs(...)` / `M.evalf(n)` | 范数 / 代入 / 数值化 |
| `M.hstack(...)` / `M.vstack(...)` | 水平/竖直拼接（增广矩阵） |
| `M.free_symbols` / `M.shape` / `M.rows, M.cols` | 含有的符号 / 形状 |

```python
xs = sp.symbols('x1:4')
f1, f2 = x1**2 + x2, sp.sin(x2) + x3
J = sp.Matrix([f1, f2]).jacobian(xs)                 # 牛顿法迭代要用的雅可比
A = sp.Matrix([[2, 1], [1, 3]])
A.eigenvals()                                        # {2: 1, 3: 1}（例）
P, D = A.diagonalize()
aug = A.row_join(sp.Matrix([5, 6])); aug.rref()      # 增广后化简
```

## 2.6 实用工具

### `lambdify` — 符号表达式 → 数值函数（衔接 scipy/numpy 的桥梁，必会）

```python
sp.lambdify(args, expr, modules=None, printer=None, use_imps=True,
            dummify=False, cse=False)
```

| 参数 | 作用 |
| --- | --- |
| `args` | 自变量，如 `(x, y)` 或 `[x]` |
| `modules` | `'numpy'`（**数组运算/向量化，默认首选**）、`'math'`（快但只接受标量）、`'mpmath'`（高精度）、`['scipy', 'numpy']`（含特殊函数 bessel 等时） |

**返回值**：普通 Python 函数，可直接喂 ndarray

```python
f_num = sp.lambdify((x, y), sp.sin(x)*sp.exp(-y), modules='numpy')
vals = f_num(np.linspace(0, 6, 100), 0.5)          # 100 个点一次算完
```

### `latex` — 输出 LaTeX 公式（论文排版）

```python
sp.latex(expr, *, mode='plain', mul_symbol=None, mat_delim='[', ...)
```

- `mode`：`'plain'`（默认行内）/`'inline'`（$...$）/`'equation'`（equation 环境）/`'equation*'`
- **返回值**：LaTeX 字符串，直接粘进论文

```python
print(sp.latex(sp.integrate(sp.exp(-x**2), (x, 0, sp.oo)), mode='equation'))
```

### 分段、条件与杂项

| 函数 | 说明 |
| --- | --- |
| `sp.Piecewise((v1, cond1), (v2, True))` | 分段函数；配合 `lambdify(modules='numpy')` 可向量化 |
| `sp.Min(a, b)` / `sp.Max(a, b)` / `sp.Abs(x)` / `sp.sign(x)` | 基本函数（Abs 求导产出 Subs，慎 diff） |
| `sp.floor(x)` / `sp.ceiling(x)` / `sp.root(x, n)` / `sp.sqrt(x)` | 取整 / n 次根 |
| `sp.factorial(n)` / `sp.binomial(n, k)` / `sp.factorint(n)` / `sp.gcd / sp.lcm` | 数论工具 |
| `sp.Interval(a, b)` / `sp.FiniteSet(*x)` / `sp.Union / sp.Intersection` | 集合（配合 solveset） |
| `sp.checksol(eq, sol)` | 验证解是否正确 |
| `sp.pprint(expr)` | 控制台二维美观打印 |

### 绘图（需 matplotlib）

```python
sp.plot(expr, (x, -5, 5), show=True)                       # 一元曲线
from sympy.plotting import plot3d                           # ⚠️ plot3d 在子模块
plot3d(sp.sin(x)*sp.cos(y), (x, -3, 3), (y, -3, 3))
sp.plot_implicit(sp.Eq(x**2/4 + y**2/9, 1), (x, -5, 5), (y, -5, 5))   # 隐函数/不等式区域
```

**返回值**：`Plot` 对象 —— `p = plot(..., show=False); p.show(); p.save('f.png')`；`plot_implicit` 还能画不等式区域（阴影）。

---

# 三、PyTorch

> 建模中的用途：① 预测类问题（回归/分类/时间序列）用神经网络；② GPU 加速大规模数值计算；③ 用自动求导解无解析梯度的优化问题。赛题中没有 GPU 或数据量小（<10⁴ 样本）时，sklearn/np 往往更省事。

## 3.0 设备与随机种子（每个脚本开头）

```python
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')   # GPU 可用性检测
torch.manual_seed(seed)          # CPU 种子；GPU 还需 torch.cuda.manual_seed_all(seed)
torch.cuda.get_device_name(0)    # 查看 GPU 型号
```

## 3.1 张量创建

| 函数签名（缩写） | 作用 | 返回值 |
| --- | --- | --- |
| `torch.tensor(data, *, dtype=None, device=None, requires_grad=False)` | **从列表/ndarray 拷贝创建**（最常用）；`requires_grad=True` 开启梯度追踪 | Tensor |
| `torch.from_numpy(ndarray)` | 从 numpy 创建，**与原数组共享内存**（改一个另一个变） | Tensor |
| `torch.zeros(*size) / torch.ones(*size)` | 全 0/全 1；`size` 可传元组 `zeros((3,4))` 或 `zeros(3,4)` | Tensor |
| `torch.full(size, fill_value)` / `torch.empty(*size)` | 全填 value / 未初始化 | Tensor |
| `torch.zeros_like(t) / ones_like(t) / rand_like(t) / randn_like(t)` | 按 t 的形状/设备/类型创建（**写到 GPU 上不换设备**） | Tensor |
| `torch.arange(start=0, end, step=1)` / `torch.linspace(start, end, steps=100)` | 等差序列 | Tensor |
| `torch.rand(*size)` / `torch.randn(*size)` | U[0,1) 均匀 / N(0,1) 正态随机 | Tensor |
| `torch.randint(low=0, high, size)` / `torch.randperm(n)` | 随机整数 / 随机排列（打乱索引用它） | Tensor（long） |
| `torch.eye(n, m=None)` | 单位阵 | Tensor |
| `torch.meshgrid(*tensors, indexing='ij')` | 网格坐标（画等高线/扫描参数用） | 各维坐标 Tensor 元组 |

```python
X = torch.tensor(np.load('X.npy'), dtype=torch.float32, device=device)
w = torch.randn(3, 5, device=device, requires_grad=True)
idx = torch.randperm(100)                       # 训练集打乱
```

> dtype 常用：`torch.float32`(默认/最快)、`torch.float64`(高精度)、`torch.long`(整数索引/标签)。转换：`t.float() / t.double() / t.long()` 或 `t.to(dtype)`。

## 3.2 形状变换与索引

| 函数签名（缩写） | 作用与返回值 |
| --- | --- |
| `t.view(*shape)` / `t.reshape(*shape)` | 改形状（共享数据）；**view 要求内存连续**，报错就换 reshape |
| `t.flatten(start_dim=0, end_dim=-1)` | 摊平（CNN → 全连接前 `t.flatten(1)`） |
| `t.squeeze(dim=None)` / `t.unsqueeze(dim)` | 去掉/添加长度为 1 的维度（`unsqueeze(0)` 升 batch 维） |
| `t.transpose(dim0, dim1)` / `t.permute(*dims)` / `t.t()` | 交换两维 / 重排所有维（换到 (N,C,H,W) 顺序）/ 2D 转置 |
| `torch.cat(tensors, dim=0)` | 沿已有维度拼接（batch 合并） |
| `torch.stack(tensors, dim=0)` | **新建维度**堆叠（把多个样本堆成 batch 用它） |
| `torch.chunk(t, chunks, dim=0)` / `torch.split(t, split_size_or_sections, dim=0)` | 切块 / 按大小切 |
| `t[mask]` / `torch.masked_select(t, mask)` / `t.masked_fill(mask, value)` | 布尔索引 / 取出 / 填充（损失掩码常用） |
| `torch.where(cond, x, y)` | 逐元素条件选择（张量版三目） |
| `torch.nonzero(t)` / `t.clamp(min=None, max=None)` | 非零索引 / 上下限截断（防数值溢出） |
| `t.expand(*sizes)` / `t.repeat(*sizes)` | 广播式扩展（不复制内存）/ 平铺复制 |

```python
batch = torch.stack([t1, t2, t3])          # 3 个 (4,5) 张量 → (3,4,5)
y = model(x.unsqueeze(0))                  # 单样本加 batch 维
t2d = t.reshape(-1, 64)                    # -1 自动推算
```

## 3.3 数学运算与归并

### 矩阵乘法与广播

```python
C = torch.matmul(A, B)        # 支持广播的矩阵乘；2D 时等价 torch.mm(A, B)、运算符 A @ B
C = torch.bmm(A, B)           # 批量 2D 乘：(b, m, k) @ (b, k, n) → (b, m, n)
C = torch.einsum(equation, *operands)   # 爱因斯坦求和，一个函数写所有乘法
```

```python
torch.einsum('ij,jk->ik', A, B)              # 矩阵乘
torch.einsum('bij,bj->bi', W, x)             # 批量加权求和
torch.einsum('bnhd,bmhd->bnm', Q, K)         # 注意力打分（Transformer）
```

### 归并（聚合）运算

| 函数签名（缩写） | 返回值说明 |
| --- | --- |
| `t.sum(dim=None, keepdim=False)` / `t.mean(...)` / `t.median(dim=None)` | 沿 dim 归并；**`dim=None` 归并成标量**；`keepdim=True` 保留该维便于广播 |
| `t.std(dim=None, unbiased=True)` / `t.var(dim=None, unbiased=True)` | 标准差 / 方差 |
| `t.max(dim=None)` / `t.min(dim=None)` | **不给 dim → 返回最大值 Tensor；给 dim → 返回 `(values, indices)` 元组** |
| `t.argmax(dim=None)` / `t.argmin(dim=None)` | 最大/最小值索引 Tensor（预测取类别：`logits.argmax(dim=1)`） |
| `torch.topk(t, k, dim=None, largest=True, sorted=True)` | 返回 `(values, indices)`，前 k 大 |
| `t.sort(dim=-1, descending=False)` | 返回 `(values, indices)` |
| `torch.unique(t, return_inverse=False, return_counts=False, dim=None)` | 去重；`return_counts=True` 附带计数 |
| `t.cumsum(dim=0)` / `t.cumprod(dim=0)` / `torch.diff(t, n=1, dim=-1)` | 前缀和 / 前缀积 / 差分 |
| `torch.norm(t, p='fro', dim=None, keepdim=False)` | 范数；**向量取 p=2/p=1/p=inf，注意默认 p='fro'** |
| `torch.cov(m, rowvar=True)` / `torch.corrcoef(m)` | 协方差 / 相关系数矩阵（每行一个变量） |

```python
prob = logits.softmax(dim=1)
pred = prob.argmax(dim=1)                      # (N,) 类别预测
acc = (pred == y).float().mean().item()        # 准确率
val, idx = torch.topk(scores, k=5, dim=1)      # Top-5
```

### 线性代数（`torch.linalg.*`，均为可求导版本）

`torch.linalg.solve(A, b)`、`torch.linalg.det(A)`、`torch.linalg.inv(A)`、`torch.linalg.svd(A)` → `(U, S, Vh)`、`torch.linalg.eigh(A)` → `(w, V)`、`torch.linalg.lstsq(A, b)`、`torch.linalg.matrix_rank(A)`、`torch.linalg.pinv(A)`

### 逐元素函数

`torch.abs / torch.sqrt / torch.exp / torch.log / torch.log1p / torch.log2 / torch.pow(t, n) / torch.sign / torch.floor / torch.ceil / torch.round / torch.sigmoid / torch.tanh`；激活走 `F.`：`F.relu(x, inplace=False)`、`F.softmax(x, dim)`、`F.log_softmax(x, dim)`、`F.leaky_relu(x, negative_slope=0.01)`、`F.gelu(x)`、`F.silu(x)`

### 张量 ↔ Python/NumPy（写日志、画图必用）

```python
t.item()                       # 单元素张量 → Python 标量（ loss 记录必须用它，否则显存泄漏）
t.detach().cpu().numpy()       # 带梯度/在 GPU 的张量 → numpy（三步缺一不可）
t.tolist()                     # 任意形状 → 嵌套 list
```

## 3.4 自动求导 autograd

### 核心机制

| API | 作用 |
| --- | --- |
| `torch.tensor(..., requires_grad=True)` / `t.requires_grad_(True)` | 声明该张量需要梯度（模型参数自动带） |
| `loss.backward(gradient=None, retain_graph=None, create_graph=False)` | 反向传播；**标量 loss 直接调**，非标量需传 `gradient=同形权重` |
| `w.grad` | 读取梯度（backward 之后才有） |
| `optimizer.zero_grad(set_to_none=True)` | **每步迭代前清空梯度**（否则梯度累加） |
| `with torch.no_grad():` | 上下文内不建计算图（验证/推理/更新规则手写时省显存） |
| `t.detach()` | 返回脱离计算图的新张量（stop-gradient） |
| `torch.enable_grad()` | 在 no_grad 区域内重新开启 |

### `torch.autograd.grad` — 直接取梯度（不经过 .backward）

```python
grads = torch.autograd.grad(outputs, inputs, grad_outputs=None,
    retain_graph=None, create_graph=False, allow_unused=None,
    is_grads_batched=False, materialize_grads=False)
```

- **返回值**：与 `inputs` 对应的梯度元组；`create_graph=True` 支持**高阶导数**（物理方程正则化常用）

### 手写梯度下降解优化问题（建模中可直接抄）

```python
w = torch.tensor([1.0, 1.0], requires_grad=True)
lr = 0.1
for step in range(500):
    loss = ((X @ w - y)**2).mean()          # 任意可微目标（最小二乘/最大似然…）
    loss.backward()                         # 计算梯度
    with torch.no_grad():
        w -= lr * w.grad                    # 梯度下降（注意 no_grad 更新）
    w.grad.zero_()                          # 清梯度
```

### 梯度裁剪（RNN 训练防爆炸）

```python
torch.nn.utils.clip_grad_norm_(parameters, max_norm, norm_type=2.0)   # 按范数缩放
torch.nn.utils.clip_grad_value_(parameters, clip_value)               # 逐元素截断
# 用法：loss.backward() 之后、optimizer.step() 之前调用
```

## 3.5 nn 层与激活

### 自定义模型模板（`nn.Module`）

```python
class Net(nn.Module):
    def __init__(self, in_dim, hidden, out_dim):
        super().__init__()                      # 必须第一行
        self.net = nn.Sequential(
            nn.Linear(in_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, out_dim))
    def forward(self, x):
        return self.net(x)

model = Net(10, 64, 1).to(device)
model.parameters()          # 参数迭代器（传给 optimizer）
model.train() / model.eval()  # 训练/评估模式（影响 Dropout、BatchNorm）
list(model.state_dict().keys())  # 参数字典（保存/加载）
```

### 主要层

| 层签名（缩写） | 输入形状 → 输出形状 | 说明 |
| --- | --- | --- |
| `nn.Linear(in_features, out_features, bias=True)` | `(*, in)` → `(*, out)` | 全连接；权重形状 `(out, in)`，即 `y = x @ W.T + b` |
| `nn.Conv1d(in_channels, out_channels, kernel_size, stride=1, padding=0, dilation=1, groups=1, bias=True)` | `(N, C_in, L)` → `(N, C_out, L')` | 1D 卷积（序列/信号） |
| `nn.Conv2d(...同上, kernel_size 可为 (kh,kw))` | `(N, C_in, H, W)` → `(N, C_out, H', W')` | 2D 卷积（图像/热力图）；`H' = (H+2p-k)/s + 1` 向下取整 |
| `nn.MaxPool2d(kernel_size, stride=None, padding=0, dilation=1, return_indices=False, ceil_mode=False)` | `(N,C,H,W)` | 最大池化降维；`stride` 默认 = `kernel_size` |
| `nn.AvgPool2d(kernel_size, stride=None, padding=0, ceil_mode=False, count_include_pad=True)` | `(N,C,H,W)` | 平均池化 |
| `nn.Embedding(num_embeddings, embedding_dim, padding_idx=None)` | 任意形状 long 索引 → `(..., emb_dim)` | 查表（类别特征嵌入） |
| `nn.Dropout(p=0.5, inplace=False)` | 同形 | 训练时随机置零；**eval() 自动关闭** |
| `nn.BatchNorm1d(num_features, eps=1e-05, momentum=0.1, affine=True)` | `(N,C)` 或 `(N,C,L)` | 按批归一化；eval 时用全局统计量 |
| `nn.LayerNorm(normalized_shape, eps=1e-05, elementwise_affine=True)` | 同形 | 按特征维归一化（小 batch 更稳） |
| `nn.ReLU(inplace=False)` / `nn.Sigmoid()` / `nn.Tanh()` / `nn.LeakyReLU(negative_slope=0.01)` / `nn.Softmax(dim=None)` / `nn.GELU()` / `nn.SiLU()` / `nn.Softplus()` | 同形 | 激活层（也可用 F.* 函数式） |
| `nn.Sequential(*modules)` | 按序执行 | 快速搭网络 |

### `nn.LSTM` / `nn.GRU` / `nn.RNN` — 序列建模（时间序列预测核心）

```python
nn.LSTM(input_size, hidden_size, num_layers=1, bias=True,
        batch_first=False, dropout=0, bidirectional=False)
```

| 参数 | 作用 |
| --- | --- |
| `input_size` | 每个时间步的特征数 |
| `hidden_size` | 隐藏状态维度 |
| `num_layers` | 堆叠层数 |
| `batch_first` | **True：输入/输出形状为 `(batch, seq, feature)`（推荐显式设 True）**；False（默认）为 `(seq, batch, feature)` |
| `dropout` | 层间 dropout（num_layers=1 时无效） |
| `bidirectional` | 双向 RNN，输出维翻倍 |

**返回值**（实测形状，batch=3, seq=5, input=4, hidden=8, layers=2）：
- 调用：`output, (h_n, c_n) = lstm(x)`（可不传初始状态，默认全零）
- `output`：每个时间步的隐藏输出 `(batch, seq, num_directions*hidden)` → `(3, 5, 8)`
- `h_n` / `c_n`：最后时刻隐藏/细胞状态 `(layers*directions, batch, hidden)` → `(2, 3, 8)`
- `nn.GRU` 返回 `(output, h_n)`；`nn.RNN(input_size, hidden_size, num_layers=1, nonlinearity='tanh', ...)` 同 GRU

```python
# 取最后时刻输出接全连接回归
out, (h, c) = self.lstm(x)          # out: (B, T, H)
last = out[:, -1, :]                # (B, H)
return self.fc(last)                # (B, out_dim)
```

### 初始化

```python
nn.init.xavier_uniform_(w, gain=1.0)
nn.init.kaiming_normal_(w, a=0, mode='fan_in', nonlinearity='leaky_relu')  # ReLU 网络推荐
nn.init.constant_(t, val); nn.init.zeros_(t); nn.init.normal_(t, mean=0.0, std=1.0)
# 用法：在 __init__ 里 self.fc.apply(self._init) 递归初始化
```

## 3.6 损失函数

| 损失签名（缩写） | 适用任务 | 输入要求（关键！） |
| --- | --- | --- |
| `nn.MSELoss(size_average=None, reduce=None, reduction='mean')` | 回归 | `input`、`target` 同形状 float |
| `nn.L1Loss(reduction='mean')` | 回归（抗离群） | 同上 |
| `nn.SmoothL1Loss(reduction='mean', beta=1.0)` | 回归 | 同上（Huber 型，大误差变线性） |
| `nn.HuberLoss(reduction='mean', delta=1.0)` | 回归 | 同上 |
| `nn.CrossEntropyLoss(weight=None, ignore_index=-100, reduction='mean', label_smoothing=0.0)` | **多分类** | `input` 是**未 softmax 的 logits** `(N, C)`，`target` 是类别索引 `(N,)` long |
| `nn.BCELoss(weight=None, reduction='mean')` | 二分类 | `input` 必须是 **sigmoid 后的概率**，target 0/1 float |
| `nn.BCEWithLogitsLoss(weight=None, reduction='mean', pos_weight=None)` | 二分类（推荐） | `input` 是 logits（内部自动 sigmoid）；`pos_weight` 处理类不平衡 |
| `nn.NLLLoss(weight=None, ignore_index=-100, reduction='mean')` | 多分类 | `input` 是 log-prob（配 `F.log_softmax`） |

- `reduction`：`'none'`（逐元素，用于加权）/`'mean'`/`'sum'`
- 函数式等价：`F.mse_loss(input, target, reduction='mean', weight=None)`、`F.cross_entropy(input, target, weight=None, ignore_index=-100, reduction='mean', label_smoothing=0.0)`、`F.binary_cross_entropy_with_logits(...)`

```python
criterion = nn.CrossEntropyLoss(weight=torch.tensor([1.0, 3.0], device=device))  # 类别加权
loss = criterion(logits, y_long)
mse = nn.MSELoss(reduction='mean')
loss = mse(pred.squeeze(-1), y.squeeze(-1))      # 回归注意形状对齐
```

## 3.7 优化器与学习率调度

### 优化器

```python
optim.SGD(params, lr=0.001, momentum=0, dampening=0, weight_decay=0, nesterov=False)
optim.Adam(params, lr=0.001, betas=(0.9, 0.999), eps=1e-08, weight_decay=0, amsgrad=False)
optim.AdamW(params, lr=0.001, betas=(0.9, 0.999), eps=1e-08, weight_decay=0.01, amsgrad=False)
optim.RMSprop(params, lr=0.01, alpha=0.99, eps=1e-08, weight_decay=0, momentum=0, centered=False)
optim.Adagrad(params, lr=0.01, lr_decay=0, weight_decay=0, initial_accumulator_value=0, eps=1e-10)
```

- `params`：`model.parameters()`；或分组字典列表 `[{'params': base, 'lr': 1e-3}, {'params': head, 'lr': 1e-2}]`
- `weight_decay`：L2 正则；小数据建模建议 Adam + `weight_decay=1e-4`
- ⚠️ 新版 SGD 默认 `lr=0.001`；老教程中 SGD 的 lr 是必填项，显式给出最稳妥
- **三连调用**：`optimizer.zero_grad()` → `loss.backward()` → `optimizer.step()`
- 运行中调参：`optimizer.param_groups[0]['lr'] = new_lr`

### 学习率调度器

```python
lrs.StepLR(optimizer, step_size, gamma=0.1, last_epoch=-1)                  # 每 step_size 个 epoch ×gamma
lrs.MultiStepLR(optimizer, milestones, gamma=0.1)                           # 到达里程碑 ×gamma
lrs.ExponentialLR(optimizer, gamma, last_epoch=-1)                          # 每 epoch ×gamma
lrs.CosineAnnealingLR(optimizer, T_max, eta_min=0.0)                        # 余弦退火，T_max=总 epoch
lrs.ReduceLROnPlateau(optimizer, mode='min', factor=0.1, patience=10,
                      threshold=0.0001, threshold_mode='rel', cooldown=0, min_lr=0)
```

- 调用时机：**每个 epoch 结束 `scheduler.step()`**（`ReduceLROnPlateau` 要传验证指标：`scheduler.step(val_loss)`）
- PyTorch ≥1.1 的固定顺序：`optimizer.step()` 在前，`scheduler.step()` 在后

## 3.8 数据加载

```python
torch.utils.data.TensorDataset(*tensors)            # 多个张量按第一维配对成样本
torch.utils.data.DataLoader(dataset, batch_size=1, shuffle=None, sampler=None,
    batch_sampler=None, num_workers=0, collate_fn=None, pin_memory=False,
    drop_last=False, timeout=0, worker_init_fn=None, multiprocessing_context=None,
    generator=None, *, prefetch_factor=None, persistent_workers=False, in_order=True)
torch.utils.data.random_split(dataset, lengths, generator=None)   # lengths=[7,3] 或比例
```

| 参数 | 作用 |
| --- | --- |
| `batch_size` | 批大小（GPU 显存不够就调小） |
| `shuffle=True` | 训练集必开（每个 epoch 重新打乱） |
| `num_workers` | 多进程读取（Windows 下用 >0 需把训练代码包在 `if __name__ == '__main__':` 里） |
| `drop_last` | 丢弃不满一批的尾部样本 |
| `pin_memory=True` | 配合 `.to(device, non_blocking=True)` 加速 GPU 拷贝 |

**返回值**：`DataLoader` 可迭代，每次 yield 一个 batch；`random_split` 返回 `[Subset, ...]`

```python
ds = TensorDataset(torch.tensor(X, dtype=torch.float32), torch.tensor(y, dtype=torch.float32))
n_tr = int(0.8 * len(ds))
train_ds, val_ds = torch.utils.data.random_split(ds, [n_tr, len(ds)-n_tr])
loader = DataLoader(train_ds, batch_size=32, shuffle=True)
for xb, yb in loader: ...                    # xb: (32, feat_dim)
```

自定义数据集（文件读取类数据）：

```python
class MyDS(torch.utils.data.Dataset):
    def __init__(self, X, y): self.X, self.y = X, y
    def __len__(self): return len(self.X)
    def __getitem__(self, i): return self.X[i], self.y[i]
```

## 3.9 保存 / 加载与推理

```python
torch.save(model.state_dict(), 'model.pt')                    # 推荐：只存参数
torch.save({'model': model.state_dict(), 'epoch': e}, 'ckpt.pt')  # 断点续训存字典
model.load_state_dict(torch.load('model.pt', map_location=device)) # 读回（先建同结构模型）
torch.load('any.pt', weights_only=False)                      # ⚠️ 2.6+ 默认 weights_only=True，
                                                              # 加载含任意对象的文件需显式 False（仅信任来源时）
model.eval()
with torch.no_grad():                                          # 推理固定写法
    pred = model(X_test.to(device)).cpu().numpy()              # → numpy 画图/写结果
```

> 混合精度（大模型训练提速）：`with torch.autocast(device_type='cuda', dtype=torch.float16):` 内做前向，配合 `torch.amp.GradScaler('cuda')` 缩放梯度。

## 3.10 完整训练模板（回归任务，可直接套用）

```python
import torch, numpy as np
import torch.nn as nn

device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
torch.manual_seed(42); np.random.seed(42)

# 1) 数据
X = torch.tensor(X_np, dtype=torch.float32)
y = torch.tensor(y_np, dtype=torch.float32).view(-1, 1)      # 回归 y 变列向量
n = len(X); idx = torch.randperm(n)
n_tr = int(0.8*n)
train_loader = torch.utils.data.DataLoader(
    torch.utils.data.TensorDataset(X[idx[:n_tr]], y[idx[:n_tr]]),
    batch_size=64, shuffle=True)
X_val, y_val = X[idx[n_tr:]].to(device), y[idx[n_tr:]].to(device)

# 2) 模型 / 损失 / 优化器
model = nn.Sequential(nn.Linear(X.shape[1], 128), nn.ReLU(), nn.Dropout(0.2),
                      nn.Linear(128, 1)).to(device)
criterion = nn.MSELoss()
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3, weight_decay=1e-4)
scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(optimizer, factor=0.5, patience=10)

# 3) 训练 + 早停
best, patience, history = float('inf'), 0, []
for epoch in range(300):
    model.train()
    for xb, yb in train_loader:
        xb, yb = xb.to(device), yb.to(device)
        optimizer.zero_grad(set_to_none=True)
        loss = criterion(model(xb), yb)
        loss.backward()
        optimizer.step()
    model.eval()
    with torch.no_grad():
        val_loss = criterion(model(X_val), y_val).item()
    history.append(val_loss)
    scheduler.step(val_loss)
    if val_loss < best - 1e-5: best, patience = val_loss, 0
    else:
        patience += 1
        if patience >= 30: break                 # 早停

# 4) 预测输出
model.eval()
y_pred = model(X.to(device)).cpu().detach().numpy()
```

> 时间序列换模型：把 `nn.Sequential` 换成含 `nn.LSTM(batch_first=True)` 的 Module，取 `out[:, -1, :]` 接输出层；分类任务把损失换成 `CrossEntropyLoss`、输出 `out_dim=类别数`、目标转 long。

## 3.11 常见坑速查

| 坑 | 解决 |
| --- | --- |
| `view` 报 "not contiguous" | 用 `reshape` / 先 `t.contiguous()` |
| CrossEntropyLoss 收敛极差 | 输入别先过 softmax（该损失内部自带） |
| LSTM 输出形状对不上 | 检查 `batch_first=True` 是否显式设置 |
| loss 不下降 | 梯度没清（忘 `zero_grad`）、lr 太大/太小、标签形状不对（回归要 `view(-1,1)`） |
| 训练越跑越慢/显存爆 | 循环里累积了计算图：记录 loss 用 `.item()`；验证用 `no_grad()` |
| `.to(device)` 没生效 | 它返回新张量，必须 `x = x.to(device)` 重新赋值 |
| numpy 与 tensor 混用报错 | `t.numpy()` 前先 `.detach().cpu()`；`torch.from_numpy` 与源数组共享内存 |
| 结果不可复现 | 统一设 `torch.manual_seed` + `np.random.seed`，DataLoader 不用 `num_workers>0` |
| Windows 多进程 DataLoader 卡死 | 训练代码放进 `if __name__ == '__main__':` |

---

# 四、附录

## 4.1 建模场景 → 函数映射表

| 建模场景 | 首选工具 | 备选/扩展 |
| --- | --- | --- |
| 线性规划 | `scipy.optimize.linprog` | `milp`（含整数变量） |
| 整数/混合规划 | `scipy.optimize.milp` | `differential_evolution(integrality=)` |
| 非线性规划（带约束） | `minimize(method='SLSQP')` | `trust-constr`、`differential_evolution`（全局） |
| 指派/匹配 | `linear_sum_assignment` | 转化为 milp |
| 曲线拟合 | `curve_fit` | `least_squares`（鲁棒）、`UnivariateSpline(s>0)` |
| 微分方程仿真 | `solve_ivp` | `odeint`、`solve_bvp`（边值） |
| 微分方程参数反演 | `solve_ivp` + `least_squares` | sympy `dsolve` 解析解 + `curve_fit` |
| 定积分/概率计算 | `quad / dblquad` | `simpson`（离散采样） |
| 插值补全/加密数据 | `interp1d / CubicSpline` | `griddata`（散点）、`RBFInterpolator`（高维） |
| 一元回归 | `stats.linregress` | `linalg.lstsq`、`np.polyfit` |
| 多元回归/降维 | `linalg.lstsq` / `linalg.svd`（手写 PCA） | torch MLP |
| 假设检验（均值/方差/分布） | `stats.ttest_*, levene, shapiro, f_oneway, chi2_contingency` | 非参数：`mannwhitneyu, kruskal, wilcoxon` |
| 聚类 | `scipy.cluster.hierarchy.linkage + fcluster` | `kmeans2` |
| 综合评价（AHP/熵权/主成分） | `linalg.eigh / eig`、`stats.entropy` 手写 | — |
| 时间序列预测 | torch `LSTM/GRU` | `statsmodels`（未在本文范围） |
| 大规模预测（回归/分类） | torch MLP + `MSELoss/CrossEntropyLoss` | sklearn |
| 公式推导/解析解 | sympy `solve/dsolve/integrate/diff` | `lambdify` 转数值 |
| 图优化（最短路/TSP） | `scipy.sparse.csgraph`（`shortest_path/dijkstra`） | networkx / milp |
| GPU 大规模数值模拟 | torch 张量运算 + `autograd.grad` | `solve_ivp` 的 `vectorized` |

## 4.2 NumPy 极简速查（scipy 的地基，建模必用）

| 函数 | 作用 | 函数 | 作用 |
| --- | --- | --- | --- |
| `np.array(obj, dtype=)` | 建数组 | `np.linspace(a, b, n)` / `np.arange(a, b, s)` | 等差序列 |
| `np.zeros / ones / full / eye(shape)` | 常量矩阵 | `np.random.default_rng(seed)` | 现代随机数生成器 `.random(n) .normal(m,s,n) .integers` |
| `A @ B` / `np.dot` / `np.outer` | 矩阵乘 / 点积 / 外积 | `np.linalg.solve / lstsq / eig / svd / norm / inv / det` | 线代（与 scipy 同名函数等价） |
| `np.hstack / vstack / concatenate` | 拼接 | `np.meshgrid(x, y)` | 网格坐标（配 griddata/画图） |
| `np.sum / mean / std / var(axis=)` | 归并 | `np.argmax / argmin / argsort(axis=)` | 极值/排序索引 |
| `np.where(cond, a, b)` | 条件选择 | `np.nanmean / nanmax / isnan` | 缺失值处理 |
| `np.loadtxt('f.csv', delimiter=',')` / `np.genfromtxt` | 读数据 | `np.savetxt('f.csv', arr, delimiter=',')` | 存数据 |
| `np.polyfit(x, y, deg)` | 多项式拟合 → 系数（高阶降序） | `np.polyval(p, x)` | 多项式求值 |
| `np.percentile(a, q)` / `np.median` | 分位数/中位数 | `np.corrcoef(x)` / `np.cov(x)` | 相关系数/协方差矩阵 |
| `np.clip(a, lo, hi)` | 截断 | `np.unique(a, return_counts=True)` | 去重计数 |

## 4.3 版本与运行说明

- 本速查表签名基于 **scipy 1.18 / sympy 1.14 / torch 2.14** 实测提取；scipy ≥1.8、sympy ≥1.12、torch ≥2.0 基本通用。
- 旧版差异速记：
  - `stats.ttest_* / shapiro / pearsonr / chi2_contingency` 旧版返回元组，新版为可解包的结果对象（取 `.statistic/.pvalue` 两不误）。
  - `differential_evolution`/`dual_annealing` 新版推荐 `rng=`（`seed=` 仍兼容）。
  - `scipy.spatial.distance.distance_matrix` 已移除 → 用 `cdist`。
  - `torch.load` 2.6 起默认 `weights_only=True`。
- 一键运行环境（uv）：

```powershell
uv run --with scipy --with sympy --with torch your_script.py
```

