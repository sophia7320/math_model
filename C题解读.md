# C 题解读：微网与外部电网电力调控策略

> 2026 CUMCM C 题。本质：**单储能微网的「购电–储能」联合优化调度问题** ——
> 确定性核心是**线性规划（LP/MILP）**，四问层层加码：
> 典型日就地优化 → 全年逐日滚动 + 紧急购电 → 光伏预报/日内调整/偏差结算 → 实时波动电价重算。
> 交付物 = 论文表 1/2/3 + 5 个严格格式的 Excel 结果文件（result1/2/3/4-2/4-3）。

---

## 1. 场景与目标（题目在描述什么）

微网 = **小区负载 + 光伏发电 + 储能设备**，通过联络线与外部电网（外网）相连，向小区供电。

- **目的**：利用负载、光伏、储能当前电量数据，给出「购电量 + 储能充放电量」，在满足小区负载的前提下**最小化购电费用**。
- **只购不售**：题目只讨论"购电"，没有售电/上网收益（弃光的剩余电量只能不被利用）。这是全文最重要的隐含简化。
- **调度粒度**：全天按 **10 分钟**分为 144 个时段（附件数据均为此粒度）；储能充放电在论文表 2 中按 **4 小时块**汇总。
- **决策量**：每时段的购电量（kWh）、储能充电量/放电量（kWh），以及由此决定的储电量轨迹。

**附录 1 储能参数（全文共用，必须背下来）：**

| 参数 | 数值 | 说明 |
| --- | --- | --- |
| 最大容量 | 12000 kWh | 额定容量 |
| 最大充/放电功率 | 5000 kW | 折算 10 分钟最多充放 **833.33 kWh/时段** |
| 初始电量 | 6000 kWh | 2025-01-01 0:00（问题 1 当天 0:00 即用它） |
| 电量安全区间 | 1200 ~ 10800 kWh | 禁止过充过放（即 SOC 10%~90%） |
| 充放电效率 | 90% | 施加口径需在假设中写明（见 §6） |
| 问题 1 末端条件 | E(0:00) = E(24:00) | 循环返回同一储电量 |

## 2. 数学骨架（四问共用）

### 2.1 记号

| 符号 | 含义 |
| --- | --- |
| t = 1..144 | 一天内的 10 分钟时段 |
| d | 日期（问题 2+ 按天滚动） |
| L_t, P_t | 时段 t 的负载、光伏功率（kW） |
| p_t | 时段 t 外网电价（元/kWh） |
| x_t | 购电量（kWh / 时段） |
| c_t, q_t | 储能充电量、放电量（kWh / 时段） |
| E_t | 时段末储电量（kWh） |
| s_t | 弃光电量（kWh / 时段），≥0 |
| e_t | 紧急购电量（kWh，问题 2+） |

> **单位换算**：功率 kW × (1/6 h) = 本时段电量 kWh。所有"功率表"转"电量结果"都乘 1/6（或 ÷6）。

### 2.2 约束（每时段）

1. **功率平衡（含弃光松弛）**：
   `P_t/6 + x_t + q_t = L_t/6 + c_t + s_t`
   等价于"微网供给 ≥ 负载"，多出的光伏可弃（s_t ≥ 0），购电量 x_t ≥ 0（不可倒卖）。
2. **储能动态**（效率口径二选一，需写明）：
   - 口径甲（效率对称）：`E_{t+1} = E_t + 0.9·c_t − q_t/0.9`
   - 口径乙（仅充电计损）：`E_{t+1} = E_t + 0.9·c_t − q_t`
3. **充放电能力**：`0 ≤ c_t ≤ 833.33`，`0 ≤ q_t ≤ 833.33`（由 5000 kW × 1/6 h 得到）。
4. **储电量安全**：`1200 ≤ E_t ≤ 10800`，`E_0 = 6000`（2025-01-01 0:00）。
5. **循环/衔接**：
   - 问题 1：`E(24:00) = E(0:00)`（即回到 6000）；
   - 问题 2+：储电量**跨日连续**（当天 24:00 即次日 0:00），不要求每天归零。
6. **充放互斥**：同一时段不应同时充、放（数学上可放松为连续 LP：同时充放只增损耗、最优解不会出现，除非结算规则制造了套利空间——务必验证）。

### 2.3 目标函数（逐问加料）

- **问题 1**：`min Σ p_t · x_t`（计划购电费）。
- **问题 2**：`min Σ p_t · x_t + 5 · Σ p_t · e_t`。
  其中紧急购电 `e_t` 只在"供给（计划购电+光伏+放电）低于负载"时触发，价格是交易时刻电价的 **5 倍**；其余费用**均按计划购电量计费**（买多了不退钱——take-or-pay）。
- **问题 3**（含计划→调整→紧急三层结算，总费用 = 计划购电费用 + 紧急购电费用 + 调整相关费用）：
  建议按"自然结算"理解（需在假设中明确）：
  `费用_t = p_t·min(x_t^plan, x_t^adj) + 0.5·p_t·(x_t^plan − x_t^adj)⁺ + 1.5·p_t·(x_t^adj − x_t^plan)⁺ + 5·p_t·e_t`
  即：调低的部分（计划高于调整）按 50% 电价支付违约费；调高的部分按 1.5 倍电价结算；不足仍触发 5 倍紧急购电。
- **问题 4**：把 p_t 换成附件 4 的**逐 10 分钟波动电价**，分别重做问题 2、3 的模型。

## 3. 数据速览（已逐项核实）

| 附件 | 结构 | 关键事实 |
| --- | --- | --- |
| 附件 1 | 1 天 × 144 槽：时间、电价、小区负载、光伏发电预测功率 | **它就是全年 365 天的逐点平均"典型日"**（与附件 2/4 的全年均值 MAE = 0.00，严格相等）；电价 0.3713~1.3952 元/kWh，20:00 前后最高（>1.3），12:00 与深夜约 0.42~0.50；负载 3309~5959 kW，白天高、夜里低；光伏峰值 7612 kW（12:00） |
| 附件 2 | 365 天 × 144 槽 × 2 表：小区负载、光伏发电**实际**功率 | 负载全年均值 ≈111 MWh/天；光伏全年发电 ≈20.3 GWh（≈55.5 MWh/天，约为负载一半），单点最大 10216 kW |
| 附件 3 | 365 天 × 4 个发布时刻（0:00/6:00/12:00/18:00）× **未来 24 个整点**预报 | "预报 k 小时" = 发布时刻 +k 小时的值（已用数据验证）；0:00 预报的日 MAE 平均约 181 kW，白天最大误差出现在午后（约 600 kW 量级），误差随提前时间增大 |
| 附件 4 | 365 天 × 144 槽：实时电价 | 0.0076 ~ 1.7936 元/kWh（波动远大于附件 1），日均价 0.54~0.93 元/kWh |
| 附件 5 | 5 个结果模板 | 结构见 §7；result2/3 覆盖 **2025-02-01 ~ 2025-12-31（334 天）** |

**四个指定报告日期（表 3）相对典型日的偏差**（后面用来讲故事/做对比）：

| 日期 | 负载 vs 年均 | 光伏 vs 年均 |
| --- | --- | --- |
| 2025-03-20 | +6.0% | −0.1% |
| 2025-06-21 | **−26.7%** | +11.9% |
| 2025-09-23 | +9.1% | +6.5% |
| 2025-12-21 | **+12.0%** | **−36.3%** |

### 格式陷阱（务必在建模时定死口径）

1. **时间标签相位差**：附件时间列写作 `00:10, 00:20, …, 0:00+1`（144 个）；结果模板时段列却写作 `0:10-0:20, …, 23:50-0:00+1, 0:00+1-0:10+1`（144 个），比附件标签整体**晚一个 10 分钟刻度**。处理办法：**按序号一一对应**（附件第 k 个数据 ↔ 结果文件第 k 个时段 ↔ 论文表格第 k 个时段），并在论文假设中明确"时间标签=时段右端点，0:00+1 表示 24:00"的约定。
2. **附件 1 与附件 2/4 的对齐**：已核验为逐点对应（shift=0 最优），即附件 1 第 k 个值就是典型日第 k 个 10 分钟槽。
3. **电量 vs 功率**：附件全是功率（kW）；结果与论文表格要电量（kWh），乘 1/6。
4. **4 小时块汇总**：储能充放电量在论文表 2/结果文件中按 `0:00-4:00, 4:00-8:00, …, 20:00-24:00` 汇总，是全天的 6 个块（问题 1）或**每天 6 个块**（问题 2+）。
5. 结果文件 result2/3 的"计划购电量"表比 result1 多两列汇总：**全天购电量、全天购电费**。

## 4. 逐问解读

### 问题 1 —— 典型日确定性调度（基础）

- **考什么**：给定附件 1（典型日的电价、负载、光伏预测），在 `供给≥负载、E 循环回归` 下，优化 144 个时段的购电量与充放电，最小化全天购电费。
- **模型**：连续 LP（变量 144×4 量级，秒解）。核心经济逻辑：**低价时段（深夜/午间）充电、高价时段（早 8-10 点、晚 18-21 点）放电**，配合光伏白天顶负荷、午间富余充电。
- **输出**：
  - 论文表 1：指定时段（10:00-10:10、12:00-12:10、14:00-14:10、16:00-16:10、18:00-18:10、20:00-20:10）购电量 + 全天购电量 + 全天购电费；
  - 论文表 2：六个 4 小时块充/放电量 + 0:00、24:00 储电量；
  - `result1.xlsx`：计划购电量（144 行）+ 充放电量（6 块）。
- **坑**：弃光是否允许（建议允许并写明）；E(0:00)=E(24:00)=6000 还是仅"相等"；表 2 的储电量是 0:00 与 24:00 两个端点值。

### 问题 2 —— 全年逐日调度 + 紧急购电（口径是关键）

- **考什么**：电价每天相同（用附件 1），负载与光伏每天不同（附件 2），在每天 0:00 制定当天计划；若实际供给不足，紧急购电 5 倍价；其余按计划购电量计费。报告 4 个指定日期（表 3）与 334 天的结果文件。
- **⚠ 信息结构是本题最大的题意歧义**（题目没有把"0:00 能知道什么"说清楚），三种口径结果差别很大，必须在论文中显式假设：

| 口径 | 0:00 计划依据 | 紧急购电结果 | 备注 |
| --- | --- | --- | --- |
| A：完美信息 | 当天实际负载/光伏（附件 2） | **恒为 0**（计划可精确匹配） | 最字面，但 5 倍价与紧急表形同虚设 |
| B：典型日期望 | 附件 1 的负载/光伏预测（=全年平均） | 偏差日非零（如 12-21 这类日） | 无需预报数据，能激活紧急机制 |
| D：0:00 光伏预报 | 负荷视为已知 + 附件 3 的 0:00 预报 | 预报误差导致的小额缺额（100~1000 kWh 量级） | 与问题 4 的数据清单（附件 2+3+4）最自洽 |

  推荐：**主模型任选 B 或 D 并在假设中写透**（倾向上，若要让"5 倍电价、紧急购电表、按计划购电计费"三条设定都有意义，B/D 优先；若严格按题面字面，A 也要作为对照/校验给出，说明紧急购电为 0 的原因）。
- **输出**：论文中 4 个日期按表 1、表 2 格式给结果 + 按表 3 格式给紧急购电结果；`result2.xlsx`（计划购电量 334 天、充放电量、紧急购电量）。
- **坑**：334 天 ≠ 365 天——**结果从 2025-02-01 开始**，1 月如何预热/初始化必须交代；储电量跨日连续；紧急购电时间段按"事件"记录（表 4 示例为 13:00-13:30 这类区间）。

### 问题 3 —— 预报驱动的计划 + 日内调整（滚动修正）

- **考什么**：每天 0:00、6:00、12:00、18:00 可拿到未来 24 小时整点光伏预报。0:00 制定计划；其他时刻根据新预报**调整**剩余时段的购电量，并对偏差结算：
  - 计划高于调整的部分：按交易时刻电价的 **50%** 付违约费；
  - 调整高于计划的部分：超出部分按 **1.5 倍**电价；
  - 供给仍不足：**5 倍**紧急购电。
- **模型**：滚动优化。0:00 用预报解一次全天计划；6:00/12:00/18:00 用新预报对"尚未执行"的时段重优化；实际执行用附件 2 实际值，储能轨迹按实际功率平推。费用按三层结算公式汇总。
- **"是否需要引入其他时刻预报"的分析**（题面原话，两种理解都要覆盖）：
  1. 评估现有 6:00/12:00/18:00 三个调整点的价值（用 vs 不用、用哪几个，做费用对比）；
  2. 讨论是否值得增加新的发布时刻/更细的滚动（可用预报误差随提前时间的曲线量化：越晚发布的预报越准，晚调整收益越大）。
- **输出**：论文 4 个日期按表 1/2/3 给结果；`result3.xlsx`（计划购电量、调整购电量、充放电量、紧急购电量）。

### 问题 4 —— 实时波动电价重算（模型泛化）

- **考什么**：把问题 2、3 的"固定电价"换成附件 4 的逐 10 分钟波动电价，重算全部结果。
- **建模变化**：目标函数电价系数逐时段变化；储能套利空间、充放时序都会变（附件 4 有极低价 ≈0.008 元/kWh 与峰值 ≈1.79 元/kWh，套利窗口更多）。模型结构不变，属于"换数据重跑 + 结果解释"。
- **输出**：`result4-2.xlsx`（问题 2 的全部结果）、`result4-3.xlsx`（问题 3 的全部结果），论文给相应计算结果。

## 5. 交付物清单（格式即分数）

| 文件 | 工作表 | 内容与粒度 |
| --- | --- | --- |
| result1.xlsx | 计划购电量 | 144 行 10 分钟时段购电量（kWh） |
| | 充放电量 | 6 个 4 小时块充/放电量 + 0:00、24:00 储电量 |
| result2.xlsx | 计划购电量 | 334 天 × 144 时段（表尾含全天购电量、购电费列） |
| | 充放电量 | 334 天 × 6 块 + 每日 0:00、24:00 储电量 |
| | 紧急购电量 | 日期、购电时间段、购电量（按事件填） |
| result3.xlsx | 计划购电量 / 调整购电量 | 各 334 天 × 144 时段（+汇总列） |
| | 充放电量 | 执行后的最终值，334 天 × 6 块 |
| | 紧急购电量 | 最终紧急购电事件 |
| result4-2.xlsx | = result2 的结构 | 波动电价版 |
| result4-3.xlsx | = result3 的结构 | 波动电价版 |

论文表：表 1（购电，含 6 个指定 10 分钟窗口 + 全天量与费）、表 2（充放电 4 小时块 + 两端储电量）、表 3（4 个日期的紧急购电）。

## 6. 题目未明确、必须自行假设的点（风险清单）

1. **问题 2 的信息结构**（A/B/D 三口径，见 §4）——影响最大，必须写进假设并做一致性说明；
2. **时间标签相位**：附件与模板差 10 分钟，按序号对齐 + 论文写明约定；
3. **储能效率施加方式**：充电乘 0.9 / 放电除 0.9（往返 81%）还是只计一侧；
4. **弃光**：建议允许（否则光伏富余时段可能无解）；表述为 s_t≥0；
5. **紧急购电计量**：按 10 分钟时段还是按事件区间汇总（模板示例按区间）；
6. **1 月的处理**：储能只给了 2025-01-01 0:00 的 6000 kWh，而结果从 2-01 开始——1 月作为预热推演还是直接用某种初值；
7. **调整的可调范围**：每次调整只允许改"尚未执行"的时段，已执行时段不可逆；
8. **问题 3 结算公式**：min 口径 vs 另加违约费口径，二选一并自洽；
9. **问题 2+ 是否要求储电每日回归**：题面只对问题 1 要求，跨日连续即可；
10. **小数位数/数字格式**：模板未规定，建议保留 2~4 位并全表统一。

## 7. 建议求解与验证路线

1. **问题 1**：直接 LP（`scipy.optimize.linprog` 或 `pulp`），144 时段 × 4 变量；可用对偶价格检查充放时段是否与"低价充、高价放"一致。
2. **问题 2/3/4**：逐日滚动 LP；334 天 × 144 时段共 ~4.8 万时段，规模很小；若担心同时充放，用连续松弛后验证，或加 0-1 变量做 MILP（`pulp`）。
3. **调整机制实现**：状态机（0:00 计划 → 6:00/12:00/18:00 各重优化剩余时段）＋ 实际执行回放（附件 2 实际功率平推储能），最后统一结算。
4. **验证（论文加分项）**：
   - 逐时段功率平衡残差 = 0、储能动态递推一致、SOC 全程在 [1200,10800]；
   - 费用重算与目标函数值一致；计划 vs 调整 vs 实际三套量的勾稽关系；
   - 用口径 A 做对照实验（紧急购电应为 0）；
   - 结果文件与附件 5 模板逐列比对（sheet 名、列名、行列数、汇总列）。
5. **出图**：电价-负载-光伏-购电-储能功率/储电量叠加时序图；SOC 轨迹；费用构成（计划/调整/紧急）饼图或堆叠；4 个日期的对比图；调整前后费用对比；预报误差分布图。

## 8. 一句话总结

> 这是一道**"带储能时序耦合的分时电价购电线性规划题"**：数学不深，但**口径多、格式严**——
> 储能动态/效率/边界、计划-调整-紧急三层结算、附件与模板的相位对齐，每一处都要"写清楚、算准确、填对格子"。
> 区分度在**建模口径的严谨性 + 全年 334 天的可复现滚动实现 + 结果文件零差错**。
