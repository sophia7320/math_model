# C 题：光伏预报误差分析（关键发现记录）

> 数据源：附件 2（光伏实际功率，365 天 × 144 槽）、附件 3（每天 0:00/6:00/12:00/18:00 发布、24 小时整点预报）
> 样本：35,004 条（365 × 4 × 24，末日晚间跨日缺 36 条）；有效光照样本约 1.7 万条
> 用途：Q2 信息口径与对冲建模、Q3 调整策略、"是否需要引入其他时刻预报"的定量论据
> 状态：探索性分析结论（脚本暂在临时目录，正式化后迁入 `solve/` 并写入 `reports/RESULTS_REPORT.md`）

---

## 0. 五个关键发现（速览）

1. **相位关系**：附件 3 的"预报 k 小时" = 发布时刻 + k 小时；错位 1 格，MAE 增大 3~7 倍；
2. **精度随提前时间近似线性衰减**：`MAE ≈ 15 + 29k`（kW，R²=0.98）；`准确率 ≈ 99.9% − 0.65%·k`；
3. **误差两段结构**：相对该(时刻, 月)期望出力的系统偏差 `μ(k)=0.5%−0.128%·k`；尺度 `σ(k)=0.06%+0.84%·k`（R²=0.97）；
4. **分布形状**：近似正态、轻微左偏 + 轻尾厚（skew≈−0.18，excess kurt≈+0.80）；t(16)/偏正态拟合优于正态；提前 19–24h 段左偏明显（skew≈−0.56）；
5. **强日内自相关**：相邻小时残差 r = 0.79/0.60/0.40/0.21（lag 5 起≈0），日级公共因子占方差 33.9%，跨日≈0 → **误差场景必须按"整日块"生成，不能逐时独立采样**。

---

## 1. 相位与对齐校验

"预报 k 小时" ↔ 实际时刻 = 发布时刻 + k 小时（位移 0）：

| 发布时刻 | 位移 −1 | **位移 0** | 位移 +1 |
| --- | --- | --- | --- |
| 0:00 | 711.5 | **181.3** | 673.3 |
| 6:00 | 661.6 | **97.5** | 655.9 |
| 12:00 | 707.4 | **207.9** | 739.9 |
| 18:00 | 713.4 | **259.9** | 700.9 |

（数值为全样本 MAE / kW；已用日 1 与多日交叉验证）

## 2. 精度随提前时间的变化

### 2.1 总指标（光照样本）

| 提前 k | MAE/kW | RMSE/kW | 偏差/kW | 准确率\* |
| --- | --- | --- | --- | --- |
| 1 h | 39 | 59 | +3 | 99.2% |
| 6 h | 181 | 297 | −10 | 95.7% |
| 12 h | 321 | 523 | −40 | 92.2% |
| 18 h | 450 | 738 | −80 | 88.8% |
| 24 h | 752 | 1276 | −209 | 82.0% |

\* 准确率 = 100% − MAPE（对实际出力 ≥ 500 kW 的光照样本，|预报−实际|/实际 的均值）

### 2.2 拟合式

- `MAE ≈ 14.8 + 29.0 × k`（kW，R² = 0.982）
- `准确率 ≈ 99.9% − 0.65% × k`（即每多提前 1 小时掉约 0.65 个百分点）

### 2.3 控制"目标时刻"后的衰减斜率（kW/小时）

| 时刻 | 7:00 | 8:00 | 10:00 | 12:00 | 14:00 | 16:00 | 18:00 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 斜率 | 9.7 | 19.7 | 38.1 | **55.7** | 39.7 | 19.1 | 5.5 |

规律：**斜率与该时刻典型出力成正比（约 0.6%~0.7%/小时）**，中午绝对误差最大。

### 2.4 更新预报的价值（同目标时段不同发布时刻的 MAE）

| 目标时段 | 0:00 发布 | 6:00 发布 | 12:00 发布 |
| --- | --- | --- | --- |
| 上午 07–12 时 | 353 | **155** | — |
| 下午 13–18 时 | 413 | 248 | **76** |

→ 越接近执行时刻的预报越准：下午用 12:00 预报比用 0:00 预报误差缩小到约 1/5.4 —— **Q3 滚动调整价值的数据依据**。

## 3. 误差结构模型（归一化后）

以 `typ(h,m)` = 该(时刻, 月份)的实际出力期望为尺度，定义 `z = (预报−实际)/typ`：

```text
系统偏差：μ(k) = 0.0050 − 0.00128 × k      （远期系统性低估，24h 约 −2.6%）
随机尺度：σ(k) = 0.0006 + 0.0084 × k      （R² = 0.966，即每小时 +0.84%）
```

| 提前 k | 1h | 6h | 12h | 18h | 24h |
| --- | --- | --- | --- | --- | --- |
| σ(k) 实测 | 1.1% | 5.7% | 10.9% | 15.2% | 24.4% |
| P(\|误差\|>10%) | 0% | 7.8% | 29.5% | 47.8% | 64.6% |
| P(\|误差\|>20%) | 0% | 0.1% | 4.5% | 16.6% | 35.4% |

> 注：24h 处实测 σ 略高于线性外推（0.244 vs 0.202），可用二次项或分段修正。

## 4. 残差分布形状（全标准化后）

统计量：skew = −0.176，excess kurtosis = +0.805（n=17,157）

**分布拟合对比（MLE，KS 用 5000 子样本）：**

| 分布 | 参数 | AIC | KS D (p) |
| --- | --- | --- | --- |
| **skew-normal** | a=−1.149, loc=0.752, scale=1.251 | 48562 | **0.019 (p=0.051)** |
| **t 分布** | ν=16.42, loc=0.011, scale=0.935 | **48518** | 0.027 (p=0.002) |
| logistic | loc=0.023, scale=0.564 | 48650 | 0.021 (p=0.024) |
| 正态 | loc=0, scale=0.999 | 48669 | 0.032 (p=9e-5) |
| laplace | loc=0.056, scale=0.785 | 49788 | 0.047 (p=6e-10) |

**标准化残差分位数**（模拟可直接用）：

| 分位 | 0.5% | 5% | 25% | 50% | 75% | 95% | 99.5% |
| --- | --- | --- | --- | --- | --- | --- | --- |
| zz | −2.74 | −1.72 | −0.64 | +0.06 | +0.68 | +1.53 | +2.55 |

**分提前段形状**：1–6h 近对称（skew +0.04）；7–12h 近对称（+0.02）；13–18h 左偏（−0.22）；19–24h 明显左偏（−0.56）——"远期偶尔深度低估"。

## 5. 相关结构（决定误差如何"进来"）

| 日内滞后 | 1 | 2 | 3 | 4 | 5 | 6 |
| --- | --- | --- | --- | --- | --- | --- |
| 自相关 r | 0.79 | 0.60 | 0.40 | 0.21 | 0.01 | 0.00 |

- 日级公共因子占方差 **33.9%**（同一天各时段误差同涨同落）；
- 跨日自相关 ≈ 0（−0.17 ~ +0.04）；
- 月度尺度基本均匀（归一化后 std 在 0.11~0.14），季节影响已由 typ(h,m) 吸收。

**含义**：一次天气过程让连续数小时同向偏差；若逐小时独立采样，会严重低估"全天累积缺口"的概率 → 场景生成用**整日误差块重采样**（见 §6）。

## 6. 可直接使用的误差模型

```text
err(k, h, m) = typ(h,m) × [ μ(k) + σ(k) · ξ ]
μ(k) = 0.0050 − 0.00128 k
σ(k) = 0.0006 + 0.0084 k
ξ ：标准化残差 ~ t(16) 或 skew-normal(−1.15, 0.75, 1.25)（简单起见正态，需写明假设）
```

**场景生成（推荐做法）——经验日块重采样：**

```text
1. 对历史每一天 d、每个发布时刻，取当日 24 小时误差向量 e_d = [err_1..err_24]
2. 场景 s：从 365 天中随机抽取一天（有放回），取其余时段误差块
3. 场景下的实际光伏：act_s(h) = fc(h) + e_d(h)（或按 lead 索引映射）
4. 重复生成 N 个场景（如 200~1000），保留日内相关与左尾
```

参数法对照：按 §6 公式逐时生成，但需人为注入相关系数（AR 结构），复杂度更高，论文中可作为对照。

## 7. 对四问建模的用法

| 问题 | 用法 |
| --- | --- |
| Q2（口径 B/D） | 把计划升级为"期望费用最小"：`min_x [计划购电费 + E_s(5×紧急购电费)]`，在"多买浪费"与"5× 罚"之间对冲；简化版用 z₉₅·σ(k)·typ 做鲁棒裕度。**注意：若采用"完全信息"口径（A），则不需要本误差模型** |
| Q3 | ① 用 σ(k) 衰减曲线量化 6:00/12:00/18:00 更新的收益（下午误差 413→76 kW）；② 调整策略要按"整日块相关"重优化剩余时段；③ 风险约束可用 CVaR 控制 5× 紧急费的尾部 |
| 论文 | 误差分布 + 精度-提前时间曲线放进"数据预处理/预报分析"一节，为不确定性建模提供依据；图表见 §8 |

## 8. 复现与产物（当前为探索版）

| 产物 | 路径 | 说明 |
| --- | --- | --- |
| 精度分析明细 | `C:\Users\sophia\AppData\Local\Temp\opencode\C_fc_skill2.txt` | 表 + 拟合 + 更新价值 |
| 精度分析图 | `...\fc_skill.png` | MAE-lead 拟合 + 时刻×lead 热图 |
| 误差分布明细 | `...\C_err_dist2.txt` | 拟合/分位数/自相关 |
| 误差分布图 | `...\err_dist2.png` | 分布+QQ+σ(k)+ACF 四联图 |
| 逐 lead 统计 | `...\err_dist2_by_lead.csv`、`fc_skill_by_lead.csv` | 可复算 |

**正式化计划（待办）**：迁入 `program/src/solve/`（拟合 + 经验日块场景生成器）、图存 `figures/`（如 `q3_forecast_skill.pdf`、`q3_error_dist.pdf`）、关键数字录入 `reports/RESULTS_REPORT.md`。

## 9. 关键数字速查（论文引用版）

- 相位：预报 k 小时 = 发布时刻 + k 小时（错位 MAE ×3~7）；
- `MAE(k) ≈ 14.8 + 29.0k` kW，R²=0.982；
- `准确率(k) ≈ 99.9% − 0.65%·k`；
- `σ(k) = 0.0006 + 0.0084k`（相对期望出力）；`μ(k) = 0.0050 − 0.00128k`；
- 尺度实测：1h 1.1% / 6h 5.7% / 12h 10.9% / 18h 15.2% / 24h 24.4%；
- 概率：P(|误差|>10%): 6h 7.8% / 12h 29.5% / 24h 64.6%；
- 分布：skew −0.18，excess kurt +0.80；t(ν=16.4) 拟合 AIC 最优区；偏正态 KS 最优；
- 相关性：日内 lag1–4 = 0.79/0.60/0.40/0.21；日级因子 33.9%；跨日≈0；
- 更新价值：下午 MAE 0:00 发 413 → 6:00 发 248 → 12:00 发 76 kW。
